# Журнал скилла: что меняли, что проверяли, что из этого выжило

Рабочий компас. Сюда смотрим перед тем, как что-то менять, и сюда же пишем результат
каждой проверки — включая отрицательный. **Отрицательный результат тут ценнее
положительного:** он единственный, что закрывает направление и экономит следующий заход.

Соседние файлы, чтобы не путаться:
| файл | что в нём |
|---|---|
| **JOURNAL.md** (этот) | история изменений и экспериментов, действующие эталоны |
| `rules_graveyard.md` | правила, которые были отменены, и чем именно опровергнуты |
| `hob_match_log.md` | реальные партии и их разбор |
| `hob_insights.md` | наблюдения из боя, применимые в следующем драфте |
| `hob_knowledge.md` | мета сета: что открыто, что переоценено |

---

## 1. Правила ведения

1. **Каждое число — с провенансом.** Откуда взято, когда посчитано, чем воспроизвести.
   Числа устаревают при правке приборов — см. § 5, где это уже случалось дважды.
2. **Утверждение без пересчёта не вносится.** В этой сессии три моих утверждения подряд
   оказались ошибкой пересказа чужого счёта. Считай сам, скриптом, до того как писать.
3. **Опровергнутое не стирается, а переносится** в `rules_graveyard.md` вместе с уроком.
4. **Число, которое опровергает устоявшееся правило, — повод проверить ПРИБОР,** а не
   правило. Дважды сработало (§ 5.2, § 5.3).
5. **n и значимость называются рядом с выводом.** 4 драфта — это проверка «не сломалось
   ли», а не направленное утверждение.

---

## 2. Действующие эталоны

**Замер 17.08.2026, после обеих правок приборов (§ 5.2, § 5.3). Выборка — 298 колод 7-1/7-2
из `ref_decks/hob/`.** Числа до 17.08 в старых заметках НЕДЕЙСТВИТЕЛЬНЫ по осям
«ломателей стойки» и «цветов».

### 2.1 Оси профиля колоды

| ось | медиана | квартили | разброс | читается как |
|---|---|---|---|---|
| существ | 14 | 13–16 | 9–21 | 🔴 требование |
| существ cmc≤2 | 6 | 5–7 | 1–11 | 🔴 требование |
| цветов (вкл. сплеш) | 2 | 2–3 | 1–4 | 🔴 требование |
| карт cmc≥5 | 3 | 2–4 | 0–9 | 🟡 ориентир |
| **пробивающих стойку** | **4** | 3–6 | 0–12 | 🟡 ориентир, разброс большой |
| связок в мейне | 3 | 2–5 | 0–13 | ⚪ паритет, см. § 4.4 |
| безусл. removal | 1 | 0–2 | 0–6 | 🟡 узко в абсолюте |
| фикс-источников | 2 | 1–4 | 0–9 | ⚪ свободный |

Отдельно: **reach** медиана 1 — это НЕ пробитие, он держит стойку (§ 5.2).

Воспроизвести: `python3 build_audit.py <лист.txt> --set hob` — печатает свои числа против
листов СВОЕЙ пары с перцентилями.

### 2.2 Цвета

| | доля из 298 |
|---|---|
| строго 2 основных цвета | **69%** |
| 3 основных цвета | 25% |
| моно | 5% |
| несут настоящий сплеш | **22%** |

Вывод: **сплеш — игра меньшинства.** «Заякорись на паре» верно. Медиана карт в сплеше — 1.

### 2.3 Шкала «насколько колода похожа на победителя»

Сумма модулей отклонений по 7 осям от медианы СВОЕЙ пары. Чтобы у числа была шкала,
посчитан собственный разброс популяции (leave-one-out, каждый лист против медианы
остальных листов своей пары, n=278):

```
среднее 8.9 · медиана 9 · квартили 7–11 · макс 21
```

Реперные точки:
| колода | отклонение |
|---|---|
| реальный драфт агента (31a78cee, WU, счёт **4:3**) | **1** |
| сами трофейные листы | 9 (медиана) |
| колоды A/B, новая версия | 10.1 (n=12) |
| колоды A/B, старая версия | 8.4 (n=12) |

⚠️ Число 10.1 против 8.4 **не является вердиктом**: обе величины внутри собственного
разброса популяции, и в выборку входят драфты с нарушенной целостностью (§ 4.2).

---

### 2.4 Ось «урон мимо блокеров» — замер 18.08.2026, n=298

Заведена после серии из 4 реальных партий BG, где две проигранные закончились с оппонентом
на 3 и на 2 жизнях. **Ни один существующий прибор эту способность не считал:**
`deck_profile.py` меряет `evasion` только у существ, поэтому `Gollum the Abandoned` (ETB −2
жизни), `The Black Arrow` (ETB 1 урон в любую цель) и `Key to the Side-Door` («не может быть
заблокировано») дают ноль по всем осям профиля.

**Определение.** Урон, проходящий при полной доске блокеров, НЕ считая эвейжн-существ:
```
DIRECT = deals \d+ damage to (any target|target player|target opponent|each opponent)
       | (each opponent|target opponent|target player|each player|defending player) loses? \d+ life
UNBL   = can't be blocked(?!\s+by)
EVA    = flying|menace|trample|can't be blocked|shadow|fear   (только Creature)
```

| ось | медиана | квартили | разброс | доля с 0 |
|---|---|---|---|---|
| урон мимо блокеров | 1 | 0–3 | 0–7 | 31% |
| эвейжн-существа | 4 | 3–6 | 0–12 | 1% |
| **СУММА** | **6** | 4–8 | 0–16 | **1%** |

**Читается как:** 🔴 требование лежит на СУММЕ (≤2 живут 11%, 0 — 1%), 🟡 разбивка между
двумя способами свободна — у 91 листа с нулём прямого урона медиана эвейжна 4.

**⚠️ Два дефекта прибора, пойманные при этом замере (§ 5 — уже третий и четвёртый случай):**
1. **Собственная потеря жизни считалась за урон в оппонента.** `Rage into the Valley`
   («**You** draw a card and lose 1 life») ловился на `loses? \d+ life` и был картой №1 роли —
   80 листов, 27%. С этим дефектом медиана роли выходила 2 вместо 1.
2. **Условное «can't be blocked by …» считалось безусловным.** `Duskwatch Hunter`
   («by tokens»), `Old Fat Spider` («by creatures with power 2 or less») попадали в роль.
Оба поймано глазами по списку совпадений ДО записи вывода — § 1 правило 2 сработало.

**Отрицательный результат, который надо помнить:** гипотеза «наша проигравшая сборка
проваливала эту ось» **НЕ подтвердилась**. Три реальные сборки прогона дали сумму 8 · 7 · 9 при
медиане популяции 6 — даже проигравшая стоит на ~75-м перцентиле.

**И второй отрицательный результат, важнее первого:** причинный вывод «победили, потому что
усилили колоду» из этого прогона тоже **не следует**. Реальных мейнов было три (по `CourseDeck`
в `Player.log`), партий на версию 1 · 1 · 3, разница между проигравшей и ушедшей 2–1 версией —
одна карта, а `Key to the Side-Door`, вокруг которого строилась половина рассуждения, не стоял
ни в одной выигравшей колоде. Итог прогона 2W–3L; третье поражение — на «усиленной» версии.

**🔴 Дефект процесса, а не прибора (18.08.2026).** Я дважды за вечер записал в постоянные файлы
утверждение о составе колоды, не читая сабмиченный декслист, — брал по памяти то, что сам
рекомендовал. Оба раза опровергнуто одним `grep` по `"CourseDeck"` в `Player.log`.
**Правило: причинный вывод о колоде пишется только после чтения реального сабмита.**

## 3. Хронология изменений

### До 16.08 — базовая версия (`cf73781`)
Один `SKILL.md` на 704 строки. Пак сортируется по одному GIH плоским списком. Баннеры:
⚑КРИВАЯ, ⚑СТОЙКА, ⚑ПЛАН, ⚑ПРОФИЛЬ, ⚑ТАЙБРЕЙК, ⚑ОСЬ. Знания из чужих колод нет.
**Это контрольная группа во всех A/B ниже.**

### 16.08 — знание из победителей
| коммит | что |
|---|---|
| `a60b3f6` | скилл разрезан на режимы (`mode_draft` / `mode_build` / `mode_analysis`); сортировка пака: группировка по кастуемости + `2×GIH + 1×IWD` вместо чистого GIH |
| `83d7f94` | GIH убран из ОЦЕНКИ колод (остался только как ось сортировки пака); `learn.py` извлекает знание из победителей сам |
| `7cb306b` | 31 трофейная колода; два моих вывода опровергнуты данными |
| `9055da4` | ⚑СВЯЗКА в момент пика; выборка расширена до 298 листов |

### 17.08 — разбор связок и проверка, работает ли всё это
| коммит | что |
|---|---|
| `04154f0` | 65 связок, тройной фильтр (механизм → lift → FDR); тройки/четвёрки отвергнуты как неотличимые от шума |
| `5480835`, `fed4f79` | хабы графа связок, интерактивная страница `hob_combos.html` |
| `575984d` | **прибор оценки починен**: сравнение с листами СВОЕЙ пары + перцентили вместо min–max по всему сету |
| `51cf4b1` | роль выше связки; `MIN_LIFT` отсекает связки, чья совместность объясняется частотой |
| `53523d5`, `42f6a62` | генератор драфта: промах в `--pool` обрывает прогон; имя двусторонней карты принимается в любой записи |
| `0a1d6e1` | **reach отделён от пробития**; дефицит роли считается проекцией, а не долей медианы |
| `8f0a872` | **связка понижена до тайбрейка**; гибрид больше не создаёт фантомный сплеш |

---

## 4. Эксперименты

### 4.1 A/B: старый скилл против нового, 8 сгенерированных драфтов
**17.08 · 16 драфтеров + 8 слепых судей, sonnet/medium · `draft_gen.py`, один сид = те же
бустеры и те же боты обеим группам.**

Контроль — не «выключенные баннеры», а физическая копия скилла на `cf73781` в отдельном
каталоге: старый `SKILL.md` одним файлом, старый `draft_live.py`. Новых файлов знания там
нет. Единственная добавка — регистрация сета HOB в `setcode()`, потому что на `cf73781`
движок про HOB ещё не знал (сантехника, не знание).

**Результат:** слепой судья отдал старой версии **6 из 8**; на 4 сидах, чистых по
целостности пула, — **3 из 4**.

**Диагноз судьи одинаков три раза из трёх** — проигрывала РОЛЬ, не связки:
> сид 42: «летунов у B всего 3 против 7 — упирается в стену на земле без плана пробить»
> сид 11: «удаления почти нет: единственный инструмент — 2× Stone by Sunlight»
> сид 58: «Thranduil даёт +1/+1 Эльфам, а Эльфов в колоде два — мёртвый текст»

Единственная победа новой версии (сид 23) — по противоположной причине: «есть реальный
пакет удаления, две бомбы и план, который работает, даже если синергия не собралась».

### 4.2 Перепроверка после правки ролей, 4 сида
**17.08 · те же сиды 11/23/42/58, где старый выиграл 3:1.**

**Результат: изменений нет.** На трёх валидных сидах старый 2 : новый 1 — ровно как было
на тех же трёх сидах до правки. Сиды 23 и 58 поменялись местами, это шум.

**Почему тест ничего не показал — ошибка отбора, моя.** Сиды брались «чистые по
целостности», а не «где роль реально проседала». На всех трёх пробивающих было 6–9 при
медиане 4 — дефицита не было, чинить нечего. Единственный сид с настоящим дефицитом (42,
3 пробивающих) в этом прогоне непригоден: там засорился контроль.

**Как ставить такой тест правильно:** отбирать сиды ПО НАЛИЧИЮ дефицита, который правка
адресует, а не по чистоте пула.

### 4.3 Насколько вообще расходятся колоды
**Замер по обоим прогонам, только драфты с целостным пулом (n=7 пар).**

При одинаковых бустерах и одинаковых ботах колоды двух версий расходятся в среднем на
**70% состава**. Даже когда обе приходят в одну пару — 33–42% расхождения.

**Вывод: знание меняет решения радикально.** Спор «есть ли эффект» закрыт: эффект есть.
Открыт вопрос, в какую сторону.

### 4.4 ❌ Связки как ось улучшения — ОТВЕРГНУТО
Ради этой оси собирались 298 колод, извлекались 65 связок, заводился баннер ⚑СВЯЗКА.

| | связок в мейне |
|---|---|
| новая версия | 3.3 |
| старая версия | 3.0 |
| 298 победителей | медиана 3.0 |

**Паритет.** Связки набираются сами, когда берёшь карты в своих цветах: механики HOB
(recruit, «вторая карта», Storied) сцеплены by design. Баннер оптимизировал ось, где
разницы нет, и вытеснял внимание с осей, где разброс реальный (пробивающих 0–12,
removal 0–6).

**Что сделано:** баннер понижен до тайбрейка — при дефиците роли не печатается вовсе, без
дефицита одна строка вместо трёх. Разбор в `rules_graveyard.md`.

### 4.5 Итоговый счёт по слепому судье
**Старый 5 : новый 2** на семи валидных сравнениях за оба прогона. Биномиальный
двусторонний **p ≈ 0.45** — неотличимо от монетки.

**Полигон смещён против новой версии, но насколько — неизвестно.** Боты в `draft_gen.py`
пикают по GIH со смещением в свои цвета плюс шум: они не читают сигналы, не пивотят и не
строят архетипы. Половина новых баннеров (⚑ПАСУЕМ, пивот, колесо) существует именно для
живого пода и против ботов не стоит ничего, тогда как цену за неё скилл платит.
**Косвенное подтверждение смещения:** реальный драфт даёт отклонение 1, а сгенерированные
8–10 — при том что сравниваются они с одной и той же популяцией.

### 4.6 Единственные реальные партии
Драфт `31a78cee`, WU, драфтил агент по НОВЫМ инструкциям → **4:3** на Арене.
По приборам колода лучшая из всего измеренного (отклонение 1). Но **n=1, контроля нет**,
и 4:3 в Premier — результат средний, не трофейный.

### 4.7 Пять линз по 298 листам: что победители делают, а скилл не знает
**17.08 · 5 линз (цвета/кривая/механики/топ-энд/«чего у нас нет») × адверсариальный
пересчёт каждого утверждения, sonnet/medium.**

**Из 15 утверждений пережили пересчёт 3.** Двенадцать отброшены скептиками — фильтр
работает, и это главное, что стоит знать о таком формате: без него в инструкцию уехало бы
вчетверо больше «находок». Все три выжившие перепроверены мной отдельно, скриптом.

**① Стоп-кран «ноль карт cmc≥6» на HOB не держится.** Правило приехало из MSH («оба
PT-листа кончаются на 5») и не переповерялось. По 298 листам: `0:104 · 1:85 · 2:69 · 3:30 ·
4:8 · 5:1 · 6:1` — **194 колоды (65%) несут хотя бы одну**, медиана 1, среднее 1.19; порог
≤2 покрывает 87%. Заменено на «≤2» в `mode_build.md § 5`, `deck_profile.py` и снято из
списка «на сборке не чинится» в `mode_draft.md`. Часть про cmc≥5 ≤4 подтвердилась.

**② GU: «доп. земельные дропы» — не дефицитная ось.** Landfall в GU — 5.32 карты на колоду,
вторая плотность в сете после BR-amass (6.54). Карт с доп. дропом всего две, и **15 из 31
трофейной GU-колоды не держат ни одной** — при этом landfall у них плотнее (5.47 против
5.19 у держащих). Механику кормит обычный дроп земли в ход. Ось снята, приоритет отдан
пейофф-телам.

**③ Old Thrush — наш локальный лик, а не стандарт сета.** Стоял в `hob_insights.md` как
«сильно недооценён» по ОДНОЙ партии. Замер: в наших WU-сборках **10 из 12 (83%)**, в
трофейных WU — **1 из 19 (5%)**. И механика записана неверно: землю он кладёт на ВЕРХ
библиотеки, маны не даёт. Статус понижен до «нишевый, не ядро WU».

**Общее правило, выведенное из ③:** баннер ⚑КРИВАЯ закрывает квоту дешёвых тел телом
ТОЛЬКО если оно есть в «Ядре пары» (`hob_patterns.md`). Квота — про роль, а не про любое
тело за 2. Иначе баннер сам порождает лик: гонит добирать двойки, а какие именно — не
проверяет.

**Метод, который сработал и стоит повторять:** линза находит → скептик обязан ОПРОВЕРГНУТЬ
пересчётом → выжившее перепроверяю я. Три ступени убрали 12 из 15. Одна ступень пропустила
бы их все.

### 4.8 Частота карт: три карты, которых победители не играют вовсе
**17.08 · `card_leaks.py` · 12 наших WU-сборок (8 драфтов) против 19 трофейных WU-листов.**

Метод: сравнение ВНУТРИ пары (карта {W}{U} редка по всему сету просто потому, что
большинство колод не WU — общесетовая частота порождает лики из ничего), присутствие в
мейне, точный тест Фишера, и отдельно — разделение «не открыли» от «открыли и не поставили»
по записанным пулам.

**`Mirkwood Meditator`, `Bard, King of Dale`, `Elrond, Moon-Reader` — 0 из 19 у победителей
при 28–40% у нас.** Не «слабоваты», а не играются вовсе теми, кто выигрывает.
`Thranduil, Sindarin Liege` 2/19 против наших 8 из 8 случаев, когда он был в пуле.

**Механизм для Thranduil — гибридная ловушка:** `{G/U}` кастуется чистой синей, флаг
кастуемости пропускает его как свою карту, а текст («другие Эльфы +1/+1») в WU мёртв —
ставили при медиане 2 Эльфа. Тот же механизм у `Mirkwood Meditator` (Landfall при медиане
2 landfall-карт). **Гибридный пип даёт доступ, а не принадлежность к архетипу.**

**Гипотеза, которая НЕ подтвердилась:** `Ori, Keeper of Songs` ставился при медиане 6
артефактов/легенд/саг — Storied включался, условие выполнялось. Карта просто слабее, чем
кажется, «мёртвый текст» тут ни при чём.

**Обратная сторона разбора оказалась пустой:** «недоигрываем» объясняется доступностью.
`Bilbo, Luckwearer` стоит у 74% победителей, но в наших пулах был 3 раза из 12 — и дважды из
трёх мы его поставили. Это про синтетический генератор, а не про нас. **Вывод про метод:
без сверки с пулом половина «ликов» — это бустер.**

**Лик общий или версионный:** `Old Thrush`/`Ori`/`Elrond` перекошены у НОВОЙ версии
(80/80/40% против 28/28/14% у старой) — их завёл баннер, лечится кодом.
`Mirkwood Meditator`/`Thranduil`/`Bard` переигрывают ОБЕ — это непонимание сета,
лечится знанием.

⚠️ n: 12 колод из 8 драфтов, наблюдения не независимы (две версии на общих бустерах),
p занижен. Направление, а не факт.

### 4.9 Лечение: третий источник рядом с GIH и ALSA
**17.08 · `find_traps.py` → `hob_traps.json` → баннер `⚠ ЛОВУШКА` / `⚠ НЕ В ЭТОЙ ПАРЕ`.**

Диагноз из § 4.8 пришлось уточнить: **гибридная версия объясняла не всё.** Пересчёт долей
по всему сету развалил её и развёл лик на два разных явления.

| карта | в сете, где кастуема | в WU | ALSA |
|---|---|---|---|
| `Bard, King of Dale` | **0 / 47** | 0/19 | **2.7** |
| `Elrond, Moon-Reader` | 5% | 0/19 | 3.0 |
| `Mirkwood Meditator` | 4% | 0/19 | 8.6 |
| `Thranduil, Sindarin Liege` | **26%** | 11% | 3.3 |
| `Ori, Keeper of Songs` | **39%** | 11% | 6.7 |

**A. Ловушка сета** — карта не играется НИГДЕ. `Bard, King of Dale` берут в среднем **2.7
пиком** и не играет **ни одна из 298 трофейных колод**. Ни GIH, ни ALSA этого не видят:
первый говорит, насколько карта выигрывает, второй — насколько рано её берут, и ни один не
говорит, **дошла ли она до мейна выигравшей колоды**. Это третий источник.

**B. Не в этой паре** — играется в сете, но не здесь. Сюда попали `Thranduil` и `Ori`, и
только для них гибридная/плотностная логика применима.

**Поправка на редкость обязательна:** медиана присутствия у рар **7%** против **30%** у
обычных. Без нормировки в «ловушки» уехали бы все рары подряд. Порог — ниже четверти
медианы своей редкости. Результат узкий и потому правдоподобный: **3 ловушки из 187 карт**
с данными.

**Проверка от независимой стороны:** парный срез, построенный совсем другим способом (доля
в сете против доли в паре), сам вытащил в WU ровно `Ori` (10%/39%) и `Thranduil` (10%/26%)
— те же карты, что нашёл `card_leaks.py` сравнением с трофейными листами.

**Что не стал строить.** Первая гипотеза лечения — общий детектор «карта хороша в сете, но
не в этой паре» с порогом ≤5%: по WU он дал **ноль кандидатов**. Строить баннер на этом
означало бы строить на шуме, поэтому порог и конструкция изменены, а не подогнаны.

Стережёт `test_trap_banner.py`, в том числе кейс B2: та же карта в СВОЕЙ паре — баннер
обязан молчать, иначе он запрещал бы карту везде.

### 4.10 Реальная партия починила счётчик ролей
**17.08 · игрок проиграл 2 из 3 партий колодам с `Bilbo, Luckwearer`: «она тянет карты, а
убрать её мне было нечем».**

Третье независимое подтверждение одного дефицита: слепой судья дважды назвал причиной
проигрыша отсутствие removal (§ 4.1), арифметика показала, что баннер об этом молчал до
32-го пика (§ 5.4), и вот то же самое из живой игры.

**Но партия вскрыла ещё и ошибку в самом счётчике.** `_HARD_RE` засчитывал только
`destroy/exile/урон в цель`, поэтому колода с тремя эффектами `-1/-1` читалась как
«removal 0» — при том что Бильбо 1/1 умирает от любого из них. Правило «−X/−0 и трюки не
removal» приехало из MSH, где ключевые тела крупные. **В HOB наоборот: из 110 существ у
победителей 54% имеют выносливость ≤2**, среди 15 самых частых крупнее 3 только двое.

Роль переименована в «ответов на тело», определение расширено С ОБЕИХ СТОРОН сравнения
(иначе счётчик молча перестал бы предупреждать): медианы по 298 листам — безусловных 1
(0–2), условных 2 (1–3), всего ответов **3 (2–5)**.

**Добавлен ПОЛ поверх проекции.** Проекция линейна, а ответы в третьем бустере разбирают:
«1 ответ на 10-м пике → к финалу 4» врёт в нашу пользу. Ноль ответов на входе во второй
бустер теперь называется независимо от арифметики — из 298 победителей с нулём ответов
живут 7 (2%), с одним и меньше 11%.

**Урок про метод:** одна реальная партия дала больше, чем 12 симулированных драфтов. Она
не подтвердила гипотезу — она указала на дефект в приборе, которого симуляция не видела,
потому что боты не эксплуатируют дыру в ролях.

---

## 5. Дефекты приборов (все найдены и починены 17.08)

Отдельный раздел, потому что **три из пяти ложных выводов этой сессии породил не анализ, а
сломанный измеритель.**

### 5.1 Сравнение со всем сетом вместо своей пары
`build_audit` сравнивал колоду с min–max по всем листам сета. Диапазоны сета шире любой
отдельной пары, поэтому «в диапазоне» получала почти каждая колода. Починено (`575984d`):
сравнение с листами своей пары + перцентили. Тогда же найдено, что гибридный пип считался
полным в оба цвета, из-за чего WB-колода читалась как WUB.

### 5.2 Reach считался пробитием
И в живом драфте (`fly + reach` против медианы 5), и в приборе (`EVASION_RE` включал
`\breach\b`). Reach блокирует летунов — он держит стойку, а не ломает.

**Цена:** на сиде 42 ПРОИГРАВШАЯ колода получала лучшее отклонение всего прогона (4.0
против 13.0 у выигравшей). После правки: пробивающих 3 (42-й перцентиль) против 9 (выше
всех победителей) — прибор говорит то же, что судья сказал вслепую.

Правильные числа: пробивающих (flying/menace/unblockable/trample) медиана 4, reach 1.
Стережёт `test_role_gaps.py`.

### 5.3 Гибридный пип создавал фантомный сплеш
`{W/U}` шёл по половине пипа в каждый цвет, поэтому карта, кастуемая одной чистой белой
маной, регистрировалась как сплеш синего.

**Цена:** из 251 «сплеша» в трофейных листах **202 (80%)** не имели ни одного чистого пипа
своего цвета, а медиана источников под них была **0** — кастовать нечем, потому что и
сплеша нет. Доля «сплешащих» победителей выглядела как 61% вместо настоящих 22%; ось
«цветов» — медиана 3 (квартили 3–4) вместо настоящих 2 (2–3).

**Едва не стало правилом** «сплешить нормально, шесть из десяти так делают» — перевёрнутым.
Отозвано до внесения. Сигналом был не спор о выводе, а невозможная величина «медиана
источников 0»: аномалия во вспомогательном числе выдала дефект раньше, чем это сделал бы
спор о самом выводе.

### 5.4 Предупреждение о removal не могло сработать раньше 32-го пика
Порог стоял «медиана × доля драфта ≥ 1.5». У removal медиана 2 → 1.5 набегает к P3P4, когда
чинить уже нечем. Заменено проекцией собственного темпа (`набрано / доля` против медианы),
с 8-го пика. Судья дважды называл причиной проигрыша отсутствие removal, а баннер за весь
драфт не сказал ни слова.

### 5.5 Генератор молча подменял пики
При промахе в `--pool` печаталось предупреждение и пак выдавался дальше — его
проигнорировали **все 16 агентов**, хотя инструкция велела остановиться. Промах каскадный:
подменённый пик меняет выбор ботов, следующие паки расходятся, и уже сделанные пики
перестают сходиться. Одна ошибка портила 2–5 пиков из 42. Теперь пак не выдаётся, `exit 2`.

Отдельно: матчер сравнивал имя с ПЕРВОЙ стороной двусторонней карты, а инструкция велела
копировать имя целиком. «Gollum, Silent Slinker // Meager Meal» не находился никогда — 17
промахов у одного агента, 12 у другого, **почти все в контрольной группе**, то есть дефект
перекосил сам результат теста. Теперь имя принимается в любой записи.

---

## 6. Что НЕ доказано

- **Что новая версия лучше старой.** 5:2 в пользу старой при p≈0.45 — это ничто, и полигон
  смещён неизвестно насколько.
- **Что новая версия хуже.** Ровно по тем же причинам.
- **Что правка ролей меняет исход.** Она делает то, для чего написана (проверено прямым
  замером), но тест этого не показал — сиды были без дефицита (§ 4.2).
- **Что 4:3 что-то значит.** n=1.

**Единственное, что решает вопрос, — реальные партии.** Симуляция может сказать «решения
изменились на 70%» и «связок не прибавилось», но не «стало лучше».

---

## 7. Что делать дальше

1. **Драфтить по новой версии** — она надмножество старой по информации, а её известная
   болезнь (связка выше роли) вылечена. У старой нет ни одного механизма, которого нет у
   новой.
2. **Вести счёт реальных драфтов в `hob_match_log.md`** с указанием версии скилла. Пять-шесть
   реальных драфтов дадут больше, чем ещё двадцать симулированных.
3. **Если снова запускать A/B — отбирать сиды по наличию дефицита**, который проверяемая
   правка адресует (§ 4.2).
4. ~~**Открытый вопрос:** какая ось на самом деле различает победителей и нас.~~
   **ЗАКРЫТ 18.08.2026 замером на контрольной группе (§ 8.6, § 8.7): маржинально —
   НИ ОДНА (все |d| ≤ 0.11); условно на составе карт оси дают +0.010 AUC, на границе
   шума. Различает качество карт (GIH d=+0.67, независимый ALSA d=−0.49).**
   Связки не добавляют ничего и здесь (§ 4.4 воспроизведён другим методом).
5. **Регенерировать `hob_traps.json`** (`python3 find_traps.py hob --write`) по мере роста
   `ref_decks/` — пороги в нём завязаны на медианы популяции, они поплывут.
6. **Самый прямой способ найти лик — считать частоту КАРТ, а не осей** (§ 4.7 ③): наши
   WU-сборки ставят Old Thrush в 83% случаев против 5% у победителей. Такое расхождение по
   конкретной карте видно сразу и лечится одной строкой, в отличие от споров про оси.
   Прогнать по всем картам, которые мы играем регулярно, — очевидный следующий заход.

---

---

## 8.2 Два дефекта приборов, найденных в живом драфте 17.08.2026

Оба — из семейства § 5: анализ был не при чём, врал измеритель. Оба найдены не тестом,
а тем, что пользователь попросил перепроверить показания.

### ① `load_cards` регистрировала только один принт из нескольких

`build_arena_ids.py` с самого начала складывал альт-арты в поле `arena_ids`, а
`draft_live.load_cards()` читала только `arena_id` — то есть **любой альт-принт становился
«НЕ РАСПОЗНАНО»** и молча выпадал из ВСЕХ счётчиков: кривой, ролей, ⚑ОСЬ, профиля.
На HOB это **60 карт из 193**, включая `An Unexpected Party`, `Bilbo, Luckwearer`,
`Glóin the Mighty`. Всплыло на базовых землях (в HOB два арт-цикла: #189–193 и #194–198),
но земли — наименее вредный случай; молча пропасть мог топовый пик.
Исправлено: регистрируются все id, 193 → **280**. Тест паритета зелёный.

### ② Проверка «работает ли пар-GIH» сравнивала данные РАЗНОГО ВОЗРАСТА

`_pair_data_is_real` сверяла свежескачанный парный файл с **локальным глобальным
снапшотом** и требовала совпадения float до 1e-9. Числа 17Lands дрейфуют: снапшот от
16.08 00:23 против кэша BG от 17.08 22:58 разошёлся по **всем 136 картам**. Защита
приняла дрейф за работу фильтра и открыла колонку `BG` — и весь живой драфт 17.08
читал глобальные числа под видом парных.

**Проверка в один момент (17.08, прямой запрос):** `colors=BG` против запроса без
фильтра — **0 из 136 расхождений**. Вывод `hob_knowledge.md` от 16.08 подтверждён:
фильтр цветов на HOB по-прежнему игнорируется.

Исправлено: вердикт считается двумя запросами, снятыми **в один момент**, и кэшируется
в `.paircheck_<set>.json` на трое суток. Кэши `cache_17l_hob_{BG,WB,WU}.json` удалены —
они содержали копию глобальных данных.

### ③ 17Lands ПЕРЕСОБРАЛ датасет HOB — выборка УМЕНЬШИЛАСЬ на 17%

Всплыло при попытке объяснить дефект ②. Тот же голый URL
(`expansion=HOB&format=PremierDraft`, без дат), одно и то же множество из 188 карт:

| | локальный файл 16.08 00:23 | live 17.08 23:20 |
|---|---|---|
| `ever_drawn_game_count` | 358 143 | **296 939** (−61 204) |
| `game_count` | 845 724 | **697 651** (−148 073) |

**У накопительных данных выборка уменьшиться не может.** Проверено, что дело не в окне
и не в рангах: `start_date` для HOB неизменна (2026-08-11T15:00Z), а `user_group=all`,
явный перечень рангов и `combine_splash` дают ровно те же 296 939. Значит 17Lands
переобработал/почистил данные, а не просто дополнил их. Причина осталась не выясненной —
это их сторона, копать дальше не стал.

**Следствие:** числа сдвинулись существенно — **69 карт из 188 на ≥1.0 GIH**
(`An Unexpected Party` 63.2 → 69.0 · `Nasty Little Rabbit` 57.7 → 61.6 ·
`Vow to Erebor` 52.9 → 47.8). Живой драфт 17.08 целиком отработал на устаревшем столбце.
Файл обновлён, старый сохранён как `17l_hob_premierdraft.bak_16aug.json`.
На сборку это не повлияло: пересчёт после обновления не поменял ни одной карты в мейне,
а тест процесса сместился +0.51 → +0.63 (норма популяции +0.49).

**Практическое правило:** `fetch_17l.py <set> --check` **не сравнивает** локальный файл с
живым — он только печатает готовность данных и никогда не пишет. Проверять свежесть
надо сравнением сумм выборки, а «не изменён» в его выводе означает лишь «я ничего не
записал».

**Урок, общий для всех трёх:** сравнение двух чисел валидно, только если совпадает не
только их смысл, но и **момент снятия**. Это уже третий прибор скилла, сломавшийся
не логикой, а рассинхроном источников.

---

## 8.1 `/effort` посреди сессии не меняет ТЕКУЩУЮ сессию (17.08.2026)

**Симптом:** гоняли `gen_feed.py hob` живьём (P1P1–P1P7), пользователь переключил `/effort
medium` → `/effort high` посреди прогона, чтобы сравнить латентность на Sonnet — разницы
не заметил.

**Причина — не в модели, а в самой команде.** Оба вывода `/effort` буквально говорят
*«saved as your default for **new sessions**»* — переключение пишет дефолт на будущее,
а не меняет эффективный уровень уже идущей сессии. Другими словами, весь прогон P1P1–P1P7
шёл на ОДНОМ эффорте (том, что был при старте сессии), несмотря на две команды `/effort`
между пиками — тест A/B не состоялся, хотя выглядел как состоявшийся.

**Второй фактор, который маскировал бы разницу, даже если бы переключение сработало:**
в этом харнессе заметная часть задержки между пиками — не reasoning, а I/O: `draft_live.py`
на каждый `watch` заново читает карты/рейтинги и рендерит блок, плюс `MTGA_SETTLE=1`
секунда дебаунса. На этом фоне разница в reasoning-бюджете medium/high может быть меньше
шума раунд-трипа.

**Правило для следующего замера effort:** переключать `/effort` МЕЖДУ сессиями, не внутри
одной — начинать новую сессию на каждом уровне, а не дёргать команду посреди прогона.
Модельный тест (Opus vs Sonnet) в § 8 этой поправки не касается — он менялся штатно
через отдельные сессии/агентов с явным `model:` параметром, а не слэш-командой посреди чата.

**Замер HOB-латентности прерван на этом пункте** — данных для вывода нет, семя(seed 7)/
состояние (`sim/gen_state.json`: P1P7, пул 6 карт BR) оставлены как есть для продолжения
в новой сессии.

---

## 8. `gen_feed.py` — блокирующий прогон на сгенерённом драфте (17.08.2026)

**Дыра, которую он закрыл.** Латентность и живой цикл проверялись только через
`draft_sim.py`, а тот переигрывает РЕАЛЬНЫЙ драфт из `Player.log` — и по HOB
переигрывать стало нечего: в `Player.log`/`Player-prev.log` драфтов не осталось,
`.draft_hist.json` перезатёрт GEN-прогонами (там только `GEN*` и обрывок `04ba02fb`),
а `pools/hob_31a78cee.txt` — это пул, паки из него не восстанавливаются. `draft_gen.py`
генерит HOB с ботами, но печатает пак в stdout, поэтому блокирующий `watch` им не
покормить. Итог: **проверить живой цикл на текущем сете было нечем.**

**Что делает.** Тот же движок `draft_gen.simulate`, но результат пишется в фейк-лог
строками вербатимного формата Arena (скопированы из лога a56fb024) →
`draft_live.find_packs` / `find_my_picks` не отличают их от настоящих.
`init` / `next` (за нас пикает бот) / `pick "Имя"` (наш пик, боты реагируют) / `status` /
`finish`. Состояние — только список наших пиков по именам; драфт каждый раз
переигрывается с нуля до координаты.

**Грабля, заложенная в конструкцию.** В `simulate` наш пик из `--pool` НЕ дёргает
`bot_pick`, а авто-пик дёргает — то есть общий `bot_rng` сдвигается в зависимости от
режима. Поэтому авто-пик записывается в состояние ПОИМЁННО, и каждый следующий пак
считается уже с обновлённым списком. Иначе состав паков после первого же авто-пика
разъехался бы с тем, что лежит в логе.

**Проверено end-to-end:** пак P1P1 отрендерился со всеми баннерами (СИГНАЛЫ, ТАЙБРЕЙК,
ОПОРА, ⚠trap), блокирующий `watch` вышел на подачу с `EXIT=0`, пул дорос и сохранился в
`pools/hob_00000007.txt` (фейковый draftId — hex, иначе `current_draft_id` его не ловит,
и реальные пулы не затираются). Ошибочное имя в `pick` печатает состав пака и не
двигает состояние.

**Чего он НЕ даёт** — того же, чего не даёт `draft_gen`: боты не читают сигналы и не
пивотят, абсолютные числа смещены. Годен для замера латентности и для сравнения двух
групп в одинаковых условиях, не годен для вывода «такая колода выигрывает».

⚠️ **Замер латентности на нём обязан идти на боевой конфигурации — Sonnet 5 / medium**
(см. врезку в SKILL.md): на Opus число непереносимо.

---

*Последнее обновление: 17.08.2026. Числа § 2 действительны для коммита `8f0a872` и позже.*

---

## 8.3 Дефект приборов, найденный на сборке живого драфта 18.08.2026

Снова семейство § 5: анализ был ни при чём, врал измеритель. Найден при обязательном
`build_audit.py` после драфта `48b3ee3d` (BR, HOB).

### ① ПОЧИНЕНО: `build_audit.G()` не находил GIH у карт-Adventure/DFC

17Lands ключует двусторонние карты по **лицевой** стороне (`Smaug, the Great Calamity`),
а декслист и `hob_set.json` — полным именем (`Smaug, the Great Calamity // Spew Flame`).
`G()` пробовала только полное имя и `split(",")[0]` → возвращала `None`, и такая карта
**молча выпадала из ОБЕИХ сторон** теста «мейн vs жадный топ-N».

**Цена на конкретном листе:** выпали все 4 `Smaug, the Great Calamity`, оба
`Gollum, Silent Slinker` и `Glóin the Mighty` — тест посчитался по 15 картам вместо 19 и
напечатал **«ОТДАНО +0.00, 🔴 МОЙ МЕЙН = ЖАДНЫЙ СПИСОК, план не выбран»**. После правки
на том же листе — **+0.93, ✅ в норме популяции**. Вердикт был перевёрнут полностью, причём
в сторону, которая заставила бы переделывать корректную колоду.

Механизм ровно тот же, что уже ловили в генераторе (§ 5.5: «Gollum, Silent Slinker //
Meager Meal не находился никогда»). Там починили, здесь — нет. **Вывод: правку имени DFC
надо было прогнать по ВСЕМ приборам разом, а не по тому, где симптом заметили.**
Не проверены на это же: `pair_gih.py`, `card_leaks.py`, `consensus.py`, `learn.py`.

### ② ПОЧИНЕНО (18.08.2026, тем же вечером): `HARD_RE` не ловил «destroy up to one … target creature»

> **Апдейт.** Правка отложена не была: страх «сдвинутся медианы популяции» **проверен и не
> подтвердился**. Новый regex по сету добавляет ровно **2 карты** (`Azog, Moria's Ruin`,
> `Celebrate the Mountain-king`), не теряет ни одной, и по 298 листам медиана оси
> «безусл. removal» остаётся **1**, а базовая линия § 2.3 — **8.9 / 9 / 7–11 / 21** без изменений.
> `test_journal.py` зелёный. Ложное срабатывание на `Elrond, Moon-Reader`
> («exile up to two other target nonland permanents **you control**» — это блинк, не removal)
> отсечено негативным lookahead на `you control`.
>
> **Цена дефекта на нашей колоде:** BR-лист с двумя Azog показывал «безусл. removal 0 —
> нижние 10% популяции» и из-за этого получал отклонение по шкале § 2.3 **10** вместо
> настоящих **8**. То есть сломанный классификатор делал корректную колоду похожей на
> аутлаера — ровно та же форма ошибки, что и в ① .
>
> **Урок для § 1 правила:** «правка сдвинет эталоны» — это гипотеза, которую надо ЗАМЕРИТЬ,
> а не основание откладывать починку. Здесь замер занял одну команду.

Исходное описание дефекта:

`deck_profile.HARD_RE = (destroy|exile) target (creature|permanent|…)` не матчит формулировку
`destroy up to one other target creature`. Из-за этого `Azog, Moria's Ruin` (безусловное
убийство любого существа) считается нулём, и колода с двумя копиями получает
**«безусл. removal 0 — нижние 10% популяции»**.

Расширенный regex `(destroy|exile) (?:up to \w+ )?(?:other )?target …` добавляет по HOB
**4 карты**: `Azog, Moria's Ruin`, `Celebrate the Mountain-king`, `Roll-Roll-Roll-Roll`,
`Elrond, Moon-Reader` (последняя — вероятно ложное срабатывание на усечённом тексте,
проверить отдельно).

**Почему не починено сразу:** ось `безусл. removal` считается и по 298 трофейным листам,
то есть правка **сдвигает медианы популяции** и обесценивает числа § 2 (правило ведения 1).
Чинить надо отдельным заходом: regex → пересчёт медиан → `test_journal.py` → обновление § 2.
До тех пор показание «безусл. removal» у колод с этой формулировкой читать вручную.

**Общий урок этих двух:** оба дефекта — не в логике, а в **сопоставлении имён и текстов**.
Третий подряд случай, когда прибор ломается на строковом матчинге, а не на арифметике.

---

## 8.4 Ответ на вопрос «не завести ли ещё осей» — ОТРИЦАТЕЛЬНЫЙ (18.08.2026)

### Замер ①: контрольной группы НЕТ и взять её негде

`fetch_trophy_decks.py` рекорд не фильтрует — только сортирует. Проверено запросом к выдаче
untapped по HOB: **545 колод, распределение 7-0: 54 · 7-1: 156 · 7-2: 335. Проигравших ноль.**

Это структурный блокер, а не неудобство: **любая новая ось отвечает на вопрос «что есть у
победителей», и ни одна не может ответить «отделяет ли это победителей от проигравших».**
Пока нет базы проигравших, новая ось описательна, а не предсказательна.

Прецедент уже записан: ось § 2.4 («урон мимо блокеров») заводилась под конкретную гипотезу
о проигранных партиях — и **гипотеза не подтвердилась**, проигравшая сборка встала на 75-й
перцентиль новой оси. Ось стоила работы и не дала различения.

### Замер ②: базовая частота дефектов приборов — 3 из 3

За один вечер проверено три прибора на ОДИН класс бага (сопоставление имён), найден во всех:

| прибор | дефект | цена |
|---|---|---|
| `build_audit.G()` | нет фолбэка на лицевую сторону DFC | «мейн = жадный, плана нет» (+0.00) → «план читается» (+0.93) |
| `deck_profile.HARD_RE` | не ловил «destroy up to one other target …» | «removal 0, нижние 10%» → «removal 2, 79-й перцентиль»; сходство 10 → 8 |
| `pair_gih.py` | нет фолбэка на лицевую сторону DFC | Adventure-карты молча выпадали из ранжирования пула (§ Шаг 0 сборки) |

Все три починены 18.08. **Непроверенными остаются `card_leaks.py`, `consensus.py`,
`learn.py`, `draft_live.py`, `replay_pick.py`.** При базовой частоте 3/3 добавлять восьмую
ось к набору с такой долей сломанных измерителей — это умножать шум, а не сигнал.

### Что делать вместо новых осей (по убыванию пользы)

1. **Найти источник проигравших колод** (публичные датасеты 17Lands — проверить, отдают ли
   они колоды независимо от рекорда). Это превращает все 7 существующих осей из
   описательных в проверяемые и, вероятно, покажет, какие из них надо **удалить**.
2. **Досмотреть оставшиеся 5 приборов** на тот же класс бага.
3. **Регресс-тест на сопоставление имён:** прогнать каждую двустороннюю карту сета через
   каждый rating-lookup. Это уже 4-й случай одного бага (первый — § 5.5).
4. Если сигнала всё же не хватает — заводить **диагностику рядом со счётом**, а не порог.
   Этот путь уже выбран для квадрантного профиля (`mode_build.md`) и по той же причине:
   восьмой порог делает цель 8+ недостижимой и ломает калибровку.

### Когда новая ось всё же оправдана — три условия разом

· она выведена из задокументированного паттерна поражений, который существующие оси
доказуемо не видят · её можно померить И на победителях, И на проигравших ·
у неё есть n, дата и **заранее названное условие опровержения**.

---

## 8.5 Досмотр всех приборов на баг сопоставления имён (18.08.2026)

Проверены оставшиеся пять по прямому указанию пользователя. **Итог: 2 чистых, 1 ложная
тревога, 2 с дефектом.** Плюс найден дефект в самом тесте — он оказался важнее находок.

| прибор | вердикт |
|---|---|
| `consensus.py` | ✅ чист — словарь ключуется лицевой И строка листа режется той же срезкой |
| `learn.py` | ✅ чист — то же самое |
| `card_leaks.py` | ✅ чист — его `norm()` сам вырезает `\s*//.*$` |
| `draft_live.py` | ⚪ рейтинги ключуются **arena_id (int)** — сопоставление имён не участвует. Но `pick_tier()` по полному имени не находил **5 из 5** двусторонних карт MSH (Tony Stark B+, Bruce Banner A, Monica Rambeau A…). В живом пути защищён вызывающими (`_name_of` уже режет `//`), поэтому дефект латентный. Фолбэк добавлен как страховка |
| `replay_pick.py` | ❌ **реальный дефект** — резолвил только лицевую. Это ровно то, что § 5.5 описывает как стоившее прогона (17 и 12 промахов у двух агентов); в генераторе починили, здесь дефект дожил |

**Побочная находка: две нормализации имён в одном скилле.** `card_leaks.norm()` вырезает
обратную сторону, `deck_profile.norm()` — нет (и не должен: `db` ключуется полным именем).
Расхождение легитимное, но именно оно и порождает весь класс багов.

**Латентная ловушка, найденная попутно (не чинил):** `draft_live.setcode()` читает **только
`sys.argv`** и молча возвращает `"sos"`. Любой импорт `draft_live` как библиотеки без кода
сета в argv тихо грузит ЧУЖОЙ сет — на этом сломался мой собственный тест, показав «карты
нет в наборе» для HOB, потому что загрузился `sos_set.json`. В боевых путях сет всегда в
argv, поэтому не трогал, но записываю: это тот же класс «тихо не тот источник».

### 🔴 Главное за вечер: тест, который не мог упасть

Первая редакция `test_dfc_names.py` была зелёной **и до, и после возврата дефекта**.
Причина: тест **повторял логику резолва у себя** вместо вызова боевого кода. То есть я
воспроизвёл ровно ту ошибку, которую весь вечер чинил в приборах, — измеритель, который
не измеряет.

Лечение — структурное, не косметическое: логика вынесена из замыканий/`main()` в именованные
функции модуля (`replay_pick.resolve_name`, `replay_pick.build_name_index`,
`build_audit.rating_of`), и тест зовёт **их**.

**Негативный контроль теперь обязателен и проведён — дефект возвращается по очереди в
каждый прибор, тест обязан упасть:**

| возвращённый дефект | тест |
|---|---|
| `replay_pick.resolve_name` | ✅ упал |
| `build_audit.rating_of` | ✅ упал |
| `draft_live.pick_tier` | ✅ упал |
| `card_leaks.norm` | ✅ упал |

`test_dfc_names.py`: **321 проверка на 4 сетах** (hob, msh, sos, mkm), все 12 тестов скилла
зелёные.

**Правило в § 1:** новый тест засчитывается только после негативного контроля — верни
дефект и убедись, что тест падает. Зелёный тест без этого ничего не доказывает.

---

## 8.6 🔴 КОНТРОЛЬНАЯ ГРУППА НАЙДЕНА — и она опрокидывает рубрикатор (18.08.2026)

Пункт №1 из § 8.4 («найти источник проигравших колод») закрыт. Результат крупный и
неудобный: **ни одна из семи осей `build_audit` не отличает победившую колоду от
проигравшей.**

### Источник

Публичные game-датасеты 17Lands. Схема (проверена, работает):
```
https://17lands-public.s3.amazonaws.com/analysis_data/game_data/game_data_public.<SET>.<EVENT>.csv.gz
```
Одна строка = одна ПАРТИЯ: `won` True/False, `deck_<карта>` — полный мейндек, плюс
`rank`, `main_colors`, `draft_id`, `build_index`, `on_play`, `opp_colors`.
Скачивает и сворачивает в колоды `fetch_17l_games.py` (стримом, файл на диск не кладётся).

· **MSH выложен:** 31 МБ gz → **377 514 партий, 83 457 колод**, из них 39 045 завершённых
  ранов (7 побед или 3 поражения): **5 113 трофейных и 10 117 провальных (≤1 победа)**.
· **HOB ещё НЕ выложен** (403). Проверять `python3 fetch_17l_games.py hob --check`.
· Ранги: platinum 16k · gold 7.3k · diamond 6.1k · silver 4.2k · mythic 3.3k · bronze 2.1k.

⚠️ Общий винрейт по ПАРТИЯМ 56.1% — это артефакт: победитель играет 7-9 партий, провал 3.
Единица анализа — КОЛОДА, а не партия.

### Замер: трофей (7 побед) против провала (≤1 победа), n=1500-2000 на группу

Оси считаны **боевым** `deck_profile.metrics` — подменён только источник строк.

| ось | трофей | провал | Cohen's d | вердикт |
|---|---|---|---|---|
| существ cmc≤2 | 5.2 | 5.2 | **+0.04** | не различает |
| ломателей стойки | 5.2 | 5.3 | **−0.02** | не различает |
| безусл. removal | 1.7 | 1.6 | **+0.10** | не различает |
| карт cmc≥5 | 2.5 | 2.5 | **+0.01** | не различает |
| существ | 14.9 | 15.0 | **−0.06** | не различает |
| фикс-источников | 2.9 | 2.8 | **+0.06** | не различает |
| цветов | 2.6 | 2.6 | **−0.04** | не различает |
| редких+мификов | 2.60 | 2.46 | +0.10 | не различает *(подтверждает старую заметку «RARE+ не коррелирует с трофеем»)* |

При n=1500 на группу это **не проблема мощности**: d=0.2 был бы виден уверенно.

### Что различает: КАЧЕСТВО КАРТ, и только оно

| метрика | трофей | провал | d | цикличность с `won` |
|---|---|---|---|---|
| средний GIH колоды | 57.84 | 56.85 | **+0.67** | 🔴 есть (GIH считается из этих же партий) |
| средний OH win rate | 56.60 | 55.57 | +0.65 | 🔴 есть |
| **средний ALSA** | **4.62** | **4.79** | **−0.49** | ✅ **НЕТ — ALSA из порядка пиков, не из исходов** |

**ALSA — решающая проверка.** Он считается по драфтам, а не по партиям, поэтому с `won`
не цикличен, и всё равно даёт почти средний эффект: у трофейных колод карты берут в
среднем на 0.17 пика раньше. Значит эффект качества карт реален, а не артефакт метрики.

**Контроль по рангу — эффект держится ВЕЗДЕ** (d по среднему GIH): bronze +0.96 ·
silver +0.85 · gold +0.77 · platinum +0.70 · diamond +0.57 · mythic +0.55.
Это не скилл игрока. Градиент содержательный: **чем выше ранг, тем МЕНЬШЕ решает
качество карт** — наверху у всех карты приличные.

### Что из этого следует, а что НЕТ

**Следует:** семь осей рубрикатора описывают, как колода УСТРОЕНА, но не предсказывают,
выиграет ли она. Ставить по ним оценку колоде — не подтверждено данными. Шкала § 2.3
(«похожесть на победителя») меряет похожесть, и только её — реперная точка про драфт
с отклонением 1 и счётом 4:3 теперь объяснена.

**НЕ следует «бери максимальный GIH каждый пик».** Тест процесса говорит, что победители
отклоняются от жадного топ-N на **+0.49 GIH**. Оба факта совместимы и вместе дают точную
картину: **у победителей карты ЛУЧШЕ, чем у проигравших, но ХУЖЕ максимально доступных.**
Качество карт — главная ось, максимизация её в лоб — нет.

### Границы

· Это **MSH**, не HOB; по HOB датасета пока нет, перепроверить обязательно.
· Трофей ≠ только колода: в 7 победах сидят и скилл, и матчапы, и удача. Контроль по
  рангу это ослабляет, но не снимает.
· Оси мерялись МАРЖИНАЛЬНО (по всей популяции). Ось может работать УСЛОВНО внутри
  архетипа и быть невидимой в среднем — это отдельная проверка, которую я не делал.
· Структура может решать в ПИЛОТИРОВАНИИ, а не в оценке колоды. Этот замер про оценку.

### Что делать дальше

1. Перепроверить на HOB, как только датасет выложат.
2. ~~Проверить оси **условно на архетипе** (внутри пары), прежде чем удалять.~~
   **ЗАКРЫТО 18.08.2026 (§ 8.8 ①): внутри пары removal даёт d=+0.20, но после вычета
   качества карт колоды остаётся +0.01…+0.10 — прокси, а не собственный сигнал.**
3. ~~Проверить, различают ли оси в верхних рангах, где качество карт решает меньше.~~
   **ЗАКРЫТО 18.08.2026 (§ 8.8 ②): в diamond+mythic картина та же, структура не
   просыпается.**
4. До этого — **не выдавать тир колоды как факт**. Рубрикатор описывает сборку, а не
   предсказывает результат, и говорить о нём надо именно так.

---

## 8.7 Потолок предсказуемости по колоде — и почему это меняет вопрос (18.08.2026)

Продолжение § 8.6. Тот замер сравнивал оси **маржинально** и дал «не различают». Здесь
модель: предсказать трофей (7 побед) против провала (≤1) по составу колоды.
n=15 230 колод MSH (5 113 / 10 117), ridge, **λ подбирается по валидации для каждой
модели отдельно** (без этого сравнение нечестное — первый прогон с фиксированным λ
дал связкам −0.09 чисто от переобучения), 6 сплитов 55/20/25.

| модель | AUC | Δ к базе |
|---|---|---|
| только качество карт (средний GIH + ALSA), 2 признака | 0.683 | — |
| **карты аддитивно (259 признаков) — БАЗА** | **0.719** | — |
| карты + 7 осей рубрикатора + кривая | 0.729 | **+0.010** |
| карты + 3160 парных связок | 0.689 | −0.030 |
| карты + оси + связки | 0.695 | −0.024 |
| **только оси + кривая, без карт** | **0.586** | −0.133 |

2σ ≈ ±0.012.

### Три вывода

1. **Потолок состава колоды — AUC ≈ 0.72.** Всё остальное (≈0.28) это пилот, матчапы,
   мулиганы, дисперсия. Сколько осей ни добавляй, выше потолка не прыгнешь.
2. **Аддитивная модель «колода = сумма карт» берёт практически весь этот потолок**
   (0.719 из 0.72). Средний GIH из двух чисел — уже 0.683.
3. **Оси добавляют +0.010 сверх карт — на границе шума, но знак устойчив** (в прогоне
   с фиксированным λ было +0.013). Это ВАЖНАЯ поправка к § 8.6: маржинально оси не
   различают (d≈0), а **условно на составе карт — чуть-чуть различают.** Именно ту
   оговорку «оси мерялись маржинально» § 8.6 и называл; проверка её подтвердила.
   Сами по себе, без карт, оси дают 0.586 — едва выше монетки.

**Связки не добавляют ничего** даже при честном подборе λ (−0.030). Это независимо
воспроизводит § 4.4 («связки как ось улучшения — ОТВЕРГНУТО») на другой выборке и
другим методом.

### Что это меняет в постановке вопроса

Вопрос «чем победившая колода отличается от проигравшей» имеет потолок 0.72 и почти
целиком отвечается качеством карт. Для СОВЕТЧИКА НА ДРАФТЕ это тупик: он и так
сортирует пак по GIH.

Рычаг в другом месте. У одного драфта **42 решения** и один исход. Мерить надо
решения, а не колоду:
· `draft_data_public.<SET>.<EVENT>.csv.gz` (MSH — 108 МБ) — пик за пиком, есть
  содержимое пака и что игрок взял. Это позволяет мерить КАЧЕСТВО ПИКА, а не колоды;
· `replay_data_public.<SET>.<EVENT>.csv.gz` (MSH — 222 МБ) — действия в партии, то
  есть пилотирование, куда `msh_insights.md` давно указывает как на боттлнек.

Ближайший дешёвый заход, не требующий новых осей: **остатки по картам** — сравнить
выученные веса аддитивной модели с GIH карты. Карты, у которых вес систематически
выше GIH, — недооценённые контекстом; ниже — переоценённые. Это готовый список,
меняющий пики, и он не требует изобретать новую ось.

---

## 8.8 Оси внутри пары, верхние ранги, остатки по картам — три хвоста § 8.6/8.7 закрыты (18.08.2026)

**Всё на MSH** (HOB-датасет проверен этим же вечером — по-прежнему 403). Выборка та же:
5 113 трофеев / 10 117 провалов из `games/msh_decks.json.gz`. Оси — боевой
`deck_profile.metrics`. **Воспроизвести одной командой: `python3 axes_vs_outcome.py msh`** —
замер § 8.7 был одноразовым и скрипта не оставил, это была ошибка провенанса; теперь скрипт
лежит в скилле и готов к HOB (§ 9).

Прибор проверен до выводов: нераспознано 56 имён на ~23 000 карт (0.24%) — бонус-лист
(Path to Exile, Sword of Fire and Ice…), которого нет в `msh_set.json`; на обе группы влияет
одинаково.

### ① Оси УСЛОВНО ВНУТРИ ПАРЫ — сигнал есть, но это прокси качества карт

Трофей против провала внутри одной пары, 11 пар с n≥100 в обеих группах:

| ось | взвеш. d | максимум по паре |
|---|---|---|
| **безусл. removal** | **+0.199** | BG +0.46 · BR +0.41 · UB +0.31 |
| фикс-источников | +0.099 | BG +0.36 · UG +0.32 |
| остальные пять | \|d\| ≤ 0.054 | — |
| GIH колоды | **+0.772** | BG +0.95 |

Внутри пары removal вроде бы «просыпается» (маржинально был +0.10). **Но после вычета
качества карт колоды сигнал умирает:** d(hard | GIH колоды) = **+0.007** взвешенно.
Под грубым и независимым от исходов прокси — ALSA колоды (сам ALSA внутри пары d=−0.44) —
остаётся **+0.101**. Честная вилка собственного вклада оси: **между +0.01 и +0.10**
(GIH цикличен и пере-вычитает, ALSA груб и недо-вычитает); решающее слово у § 8.7 —
по-карточная модель оставляет всем осям вместе +0.010 AUC. **Считать removal — значит
повторно считать «хорошие карты», отдельной информации в счётчике почти нет.**
Оговорка § 8.6 «оси мерялись маржинально» этим закрыта и по парам.

### ② Верхние ранги (diamond+mythic, 1 013 / 2 703) — структура не просыпается и там

hard +0.16 · fixers +0.15 · остальные \|d\| ≤ 0.06 · GIH +0.55. Гипотеза «наверху качество
карт решает меньше, значит структура — больше» не подтвердилась: оси-лидеры те же и такие же
маленькие, просела только ось качества (+0.55 против +0.67 по всем рангам).

### ③ Остатки по картам: поимённый список почти весь — шум, устойчив ЦВЕТОВОЙ перекос

Ridge на 264 картах (λ=30 по 5-fold CV, AUC 0.7192 — эталон § 8.7 воспроизведён),
corr(вес, GIH)=0.824, остаток веса после регрессии на GIH.

**Список из § 9 Шаг 3 п.1 в задуманном виде НЕ живёт:** сплит-половины дают corr=0.366,
из топ-15 в обеих половинах выживают **3 из 15** с каждого конца. Выжившие:
· недооценены GIH — `Captain Marvel, Earth's Protector` (+4.0σ, GIH 70.2),
  `The Super Hero Civil War` (+3.0σ, 70.3), `M.O.D.O.K.` (+2.3σ, 64.2) — **все три бомбы:
  GIH сжимает верх шкалы**, реальный вклад бомбы в трофей больше, чем говорят её ~70;
· переоценены — `Multiversal Incursion` (−2.8σ), `Captain America, Living Legend` (−2.1σ),
  `Heroic Feast` (−1.9σ).

**А вот агрегат по цвету карты устойчив на обеих половинах:**

| цвет | средний остаток | половины |
|---|---|---|
| **R** | **+0.82σ** | +0.80 / +0.74 |
| **B** | **+0.43σ** | +0.37 / +0.45 |
| G | −0.13σ | −0.13 / −0.13 |
| U | −0.38σ | −0.30 / −0.41 |
| W | −0.43σ | −0.29 / −0.51 |

**Глобальный GIH систематически завышает карты цветов сильнейшей пары и занижает карты
слабых пар.** W/U — цвета лучшей пары MSH (WU: 1 452 трофея из 5 113), R сидит в четырёх
самых редких трофейных парах. Механизм: GIH карты — среднее по колодам, в которых она
ездила, и сила архетипа протекает в число карты. Это количественная версия давно знакомого
«пар-GIH ≠ глобальный GIH», только теперь она измерена: **±0.4–0.8σ на ровном месте при
сравнении карт РАЗНЫХ цветов в паке.**

### Итог по вопросу «чем трофейная колода отличается от не-трофейной»

Все три хвоста § 8.6 «Что делать дальше» закрыты, картина едина: **качеством карт — и
почти ничем сверх.** Оси не различают ни маржинально (§ 8.6), ни в верхних рангах (②),
ни внутри пары сверх качества карт (①); по-карточная модель упирается в потолок 0.72
(§ 8.7). Что осталось живого сверх «среднего GIH»: бомбы весят больше своего GIH, и
глобальный GIH перекошен по цветам силой архетипов. Оба факта — про ПИК, не про сборку,
и оба переносить на HOB только после замера на HOB-датасете (§ 9).

---

## 8.9 Первый замер КАЧЕСТВА ПИКОВ: провальщик — это трофейщик плюс шум (18.08.2026)

Открыт путь § 8.7/§ 9 Шаг 3 п.2 — «мерить решения, а не колоду». Источник:
`draft_data_public.MSH.PremierDraft` (108 МБ gz), одна строка = один пик: весь пак,
что взято, итоговый рекорд рана, скилл-бакет игрока.
**Воспроизвести:** `python3 fetch_17l_picks.py msh` → `games/msh_picks.json.gz` (29 МБ,
мягкий фильтр: все завершённые раны) → `python3 pick_quality.py msh --stage 1|2`.
Выборка: **51 832 завершённых драфта** (51 819 полных 42/42), 2.18 млн пиков,
трофеев 7 276 · провалов (≤1 победы) 13 168.

**Этап 0, прибор:** пик лежит в паке в 2 176 931 / 2 176 931 строк; паки ровные
14-13-12…, дыр в последовательностях 0; рейтинги 17L есть у всех карт кроме 5 базовых
земель; 55 карт бонус-листа отсутствуют в `msh_set.json` (для GIH-ранжировки не мешает,
роли им не считаются — учтено признаком «бонус-лист»).

**Этап 1, счётчики (жадный GIH):** трофейщики берут топ пака по GIH в **43.5%** пиков,
провальщики — **36.7%** (+6.8 п.п.); цена упущенного 1.93 vs 2.45 GIH-пункта за пик
(×42 пика ≈ та самая разница «у трофейных карты лучше» из § 8.6). Детали, которые
важнее среднего:
· **разрыв НЕ в спорных паках:** на очевидных (отрыв топа ≥3 GIH) 64.9% vs 56.7%
  (+8.2 п.п.) — провальщики теряют на пиках, где выбор очевиден;
· **скилл объясняет чуть больше половины:** внутри среднего скилл-бакета (0.50–0.56)
  +4.3 п.п., на спорных +2.6 — эффект живёт и при равном историческом винрейте;
· держится на ALSA (+2.7 п.п.) — не цикличность GIH;
· максимум разрыва — первые пики каждого бустера (P1P1 +11.6 п.п.), к хвостам → 0.

**Этап 2, условный логит** (P(взять|пак) = softmax(w·x), 7 признаков, общий скейлер,
сплит-половины сходятся до 2-го знака):

| признак | трофей (Б1) | провал (Б1) | Δ |
|---|---|---|---|
| **GIH** | **+1.464** | **+1.064** | **+0.401** |
| в цвет пула | +0.502 | +0.474 | +0.028 |
| редкость | +0.298 | +0.311 | −0.013 |
| бонус-лист | +0.138 | +0.090 | +0.048 |
| removal | +0.171 | +0.175 | −0.004 |
| существо / cmc | +0.280 / +0.100 | +0.279 / +0.111 | ≈0 |

Бустеры 2–3: та же картина (Δ GIH +0.32 / +0.28), вес «в цвет» растёт к Б2-Б3 у обеих
групп одинаково (+0.50 → +0.62), вес GIH у обеих падает.

**Главный вывод: у провальщиков НЕ другая система ценностей — у них та же система с
бóльшим шумом.** Все веса, кроме GIH, совпадают до сотых: цветовая дисциплина, премия
за редкость, removal, тела — одинаковы. Отличие одно: трофейщик режет по качеству
карты заметно жёстче (softmax острее). Гипотезы «провальщики рейр-драфтят» (Δ редкости
−0.01) и «не держат цвета» (Δ +0.03) данными **не подтверждены**.

**Что это меняет для скилла:** советчик, максимизирующий качество карты в паке, уже
делает главное — снижает шум пика. Приоритеты менять не по чему: выигрывающие люди
весят пак так же, как проигрывающие, только точнее. Ставка скилла на «подсветить
лучший пик и не дать отвлечься» — правильная по данным.

**Этап 3 (тем же вечером): пики НЕ добавляют предсказательной силы сверх колоды.**
Джойн пиков с колодами по draft_id: 20 423 драфта. Ridge, λ по CV:
колода 0.7330 ± 0.0038 · колода + 7 пик-признаков **0.7347 (+0.0017 — ноль в шуме)** ·
пик-признаки одни **0.6428** (из них один «упущено/пик» — 0.6405).
Читается так: цена упущенного информативна, но **вся её информация уже материализована
в итоговом листе** — плохие пики становятся плохой колодой, отдельного канала «что
предлагали» сверх этого не видно. Потолок § 8.7 подтверждён с третьей стороны.

**Этап 4 (тем же вечером): экзамен ранжировщиков на пиках людей.**
Доля пиков игрока, совпавших с топом ранжировщика, спорные паки (разрыв топ-2 ≤1 GIH):

| ранжировщик | трофейщики | провальщики |
|---|---|---|
| чистый GIH | 31.8% | 27.1% |
| **наша ось 2·GIH+IWD** | **31.6%** | 27.9% |
| **логит-трофейщика (=GIH+цвет пула)** | **39.7%** | 35.6% |

Два вывода:
· **Ось тайбрейка IWD поведением победителей НЕ подтверждается.** На спорных она не
  лучше чистого GIH (31.6 vs 31.8), а точечный тест ровно в заявленном месте правила
  («при равном GIH решает IWD», `draft_live.py`, выведено 10.08.2026 по ρ с пик-тирами):
  на GIH-ничьих (|Δ|≤0.5) с различающим IWD (|Δ|≥1.5) трофейщики берут высокий IWD в
  **52.2%** (n=11.5k) — монетка, а провальщики — в **55.4%**, т.е. ЧАЩЕ. По закону
  калибровки 4 правило понижено до гипотезы: обосновано оно корреляцией с экспертными
  тирами, а не поведением победителей и не исходами. Код не менялся (вреда ось не
  показала — на «все пики» 43.3% vs 43.5%); менять ли — решение за пользователем.
· **Что реально решает ничьи у победителей — цвет пула:** добавка признака «в цвет»
  поднимает попадание в пик трофейщика с 31.8% до 39.7% (+8 п.п.). Группировка пака
  по кастуемости в `draft_live` делает ровно это и данными ПОДТВЕРЖДЕНА.

⚠️ Оговорки: (1) «очевидный пак» определён по глобальному GIH — поздний пик в цвет
вместо чужой бомбы засчитывается как отклонение, часть «ошибок на очевидном» может
быть легитимной дисциплиной; на вывод этапа 2 это не влияет (там пак целиком).
(2) Скилл-бакет 17Lands — история игрока на платформе, новичков с одной партией он
не калибрует. (3) Всё это MSH; на HOB — той же парой команд, когда выложат (§ 9).
(4) «Совпадает с трофейщиком» ≠ «лучший совет»: победители могут систематически
ошибаться там, где статистика права; но нулевой + перевёрнутый эффект IWD означает,
что ЗА тайбрейк IWD сейчас не говорит ничего, кроме корреляции с тирами.
(5) Экзамен гонял ГОЛУЮ ось 2GIH+IWD без группировки по кастуемости — полный
`draft_live` ближе к логиту, чем к голой оси; это в его пользу, не против.
Воспроизвести этапы 3–4: `python3 pick_quality.py msh --stage 3|4`.

**Правки внесены (18.08.2026, по указанию пользователя, тем же вечером):**
· **IWD снят** из оси сортировки `pack_order` и из причин ⚑ ТАЙБРЕЙК (остался пик-тир;
  сам IWD печатается как информация, `⚠trap` на IWD<0 жив). Похоронен —
  `rules_graveyard.md § 9`.
· **Новая ось: score = GIH + цветовая поправка + бомба-надбавка.** Величины переведены
  из σ-остатков § 8.8 в GIH-пункты через наклон регрессии вес~GIH (0.0078 веса/пункт):
  цвет на MSH — W −1.2 / U −1.0 / B +1.2 / R +2.3 / G −0.4 (`COLOR_GIH_ADJ`, по-сетовая,
  для HOB пусто до датасета); бомба — остатки по полосам GIH: до 63 ≈ 0, 63-66 → +3.4,
  66+ → +7.3 (Captain Marvel 70.2 ≈ 81) → в коде `1.5·(GIH−63)⁺` кап +12, печатается
  ярлыком `💣экв≈NN` (внутри группы монотонна — порядок не меняет, нужна для сравнения
  через группы кастуемости).
· Док. случай P1P2 (Take Up the Shield {W} vs Super-Skrull {B}, ничья 59.9) новая ось
  решает в ту же сторону — через цвет вместо опровергнутого IWD; `test_pack_order` и
  `test_tiebreak` обновлены, **негативный контроль проведён** (поправку выключили — тест
  упал), все 12 тестов зелёные, паритет Premier/Quick цел.

---

## 8.10 Момент коммита в цвета: тайминг НЕ решает (18.08.2026)

Проверка «стей-опен / late-commit» гипотезы (висела целью в памяти со слов стримеров).
**Воспроизвести: `python3 commit_timing.py msh`** (джойн пиков с реальными листами;
43 674 завершённых 2-цветных драфта, 3+ цвета и моно исключены — 3 293 шт.).

Определения: ЛОК-ПИК — пик, после которого топ-2 цвета пула совпадают с финальной парой
колоды до конца (43 = не залочился); СИЛА СИДЕНЬЯ — средний max-GIH по 14 пакам Б1
(свойство бустеров, не игрока) — обязательный контроль.

**① Тайминг коммита не различает трофей и провал.** Лок-пик: медиана **7** у ОБЕИХ групп
(кв. 3–16), d=−0.04; пивот после Б1 — 28.6% против 29.2%; потерянные P1-пики (не доехали
до мейна) — **5.12 против 5.11 из 14** (d=+0.005 — самое ровное совпадение за всю сессию).

**② Дозовая кривая по всем завершённым ранам — плоская в окне P1P1–P2P14:**
побед 3.31 · 3.37 · 3.34 · 3.33 (лок 1-3 / 4-7 / 8-14 / 15-28). Наказываются только
патологии: лок в Б3 — 3.21, не залочился вовсе — 3.17 (−0.15 побед, и это хвост 11%).
Внутри квартилей силы сиденья — та же плоская форма.

**③ Что реально двигает исход — сиденье.** Слабое сиденье → 2.86–3.15 побед, сильное →
3.48–3.60: **~0.5 победы** — на порядок больше любого таймингового эффекта. Сильное
сиденье само сдвигает лок раньше (медиана 6 против 8) — вот откуда легенда «умелые
коммитятся поздно/рано»: тайминг — СЛЕДСТВИЕ открытия, а не техника.

**Выводы для коучинга:** · агонизировать «когда коммититься» в окне первых двух бустеров
не стоит ничего — свободный параметр; · «держаться открытым» тоже бесплатно (все теряют
половину P1-пиков, трофейщики не меньше); · единственное правило с ценой — **войти в Б3
определившимся**; · пивот после Б1 — норма (29% всех), не элитная техника; · цель «освоить
late-commit стиль» закрыта данными: стиль не выигрывает. Единственная жёсткая оговорка —
всё это ПОВЕДЕНИЕ ПОПУЛЯЦИИ, а не эксперимент: драфтер, который лочится к P7 по СИЛЬНОМУ
открытию, прав, и тот, кто ждёт до P20 по мутному, — тоже; данные говорят лишь, что сам
выбор момента в этом окне исход не двигает.

---

## 8.11 Срез по ИГРОКАМ (топ vs низ по историческому винрейту) — картина § 8.9 подтверждена независимым делением (18.08.2026)

Деление трофей/провал обусловлено исходом одного рана — в нём сидит удача.
`user_game_win_rate_bucket` — история игрока по всем его играм, от удачи текущего
драфта не зависит. **Воспроизвести: `python3 skill_split.py msh`.**
Пороги: ТОП wr≥0.62 (4 580 драфтов), НИЗ wr≤0.46 (5 374), только 100+ игр.
Санити: топ берёт в среднем **4.57 победы**, низ — **2.23** (трофеи: 33% vs 3%) —
бакет настоящий, и вот столько стоит скилл целиком.

**Пики.** Топ берёт топ-GIH пака в 44.6% против 36.9% (+7.7 п.п. — больше, чем
трофей/провал +6.8); цена упущенного 1.81 vs 2.43; на спорных 32.5% vs 27.1%.

**Логит (Б1).** Разрыв веса GIH ещё больше, чем в исходном срезе: **+1.63 vs +1.05
(Δ+0.58** против Δ+0.40 у трофей/провал), а все остальные веса опять совпадают
до сотых (в_цвет Δ+0.007, removal Δ−0.013, редкость Δ−0.024). Низ платит за
редкость чуть больше, топ чуть охотнее берёт бонус-лист (+0.056) — крохи.

**Колоды.** Средний GIH: d=+0.68 — в точности как у трофей/провал (+0.67).
Оси — та же мелочь (cheap +0.18, hard +0.14, c5 −0.13, evasion −0.12).

**Вывод, теперь независимый от способа деления:** «у сильных не другая система
ценностей, а та же с меньшим шумом» — воспроизведено на срезе, в котором удача
текущего рана не участвует. Точность по качеству карты — это и есть измеримая
часть драфт-скилла; приоритеты у топа и низа одинаковые.

**Оговорка:** wr-бакет 17Lands может частично включать игры текущего драфта
(лёгкая утечка), и обе группы — самоотчётные пользователи 17Lands, не вся Арена.

---

## 8.12 Телеметрия советчика поставлена (18.08.2026, по указанию пользователя)

Связка «что советовала ранжировка → что взял игрок» испарялась: пул сохранялся, а
согласие с советом — нет. Теперь `draft_live.record_telemetry` (вызывается из
`render_block`, т.е. Premier и Quick одинаково) пишет на каждом пике в
`pools/<set>_<draft8>_telemetry.jsonl`: запись пака (топ-1/топ-2 первой группы
ранжировки + топ по голому GIH) и запись фактического пика; джойн pack.i == pick.i.
Дедуп по координате пака и индексу пика; как save_pool — никогда не роняет драфт.
Сводка: **`python3 telemetry_report.py <set> [draft8]`** — доля пиков по топ-1
ранжировки, по голому GIH и расхождения поимённо. Шаг 0 порядка сборки в
`mode_build.md`. Эталоны для сравнения: трофейщики MSH 43.5%, топ-игроки 44.6% (§ 8.9/8.11).

Что это даст через 10–15 реальных драфтов: собственный экзамен § 8.9 — но полного
советчика и на НАШИХ пиках, с разбором «кто был прав» по расхождениям; персональные
паттерны срывов (наш задокументированный лик — flashy-пики).

⚠ Пишется топ МЕХАНИЧЕСКОЙ ранжировки, не проза модели — прозу машинно не сверить.
`test_telemetry.py`: дедуп, джойн, устойчивость к мусору; негативный контроль
проведён в процессе (отключение дедупа → 2 записи → ассерт падает).

---

## 8.13 Постоянный учёт партий + архив логов до ротации (18.08.2026, по указанию пользователя)

Дыра: Player.log ротируется быстро, и всё неразобранное пропадало навсегда — включая
сам СЧЁТ (какая колода сколько выиграла). Держалось на ручной дисциплине «разбери сразу».

**`match_archive.py <set>`** — идемпотентно переносит из лога в скилл:
· `matches/<set>_ledger.jsonl` — партия = строка: matchID, игра, таймстамп, событие,
  seat, W/L, ходы, **сабмиченная колода поимённо** (ближайший предшествующий CourseDeck —
  правило § 2.4 о причинных выводах теперь механизировано);
· `logs/<match8>.log.gz` — сырой сегмент матча, разбор возможен после ротации (в git
  не идёт, .gitignore);
· `--status` — счёт W-L по событиям и колодам.
Чужие события (Ladder, AIBotMatch) отсекаются по InternalEventName.

**Автовызов**: `analyze_game.py` и `match_watch.py` дёргают `run_quiet()` при старте —
любая разбираемая или коучимая партия архивируется сама; после сессии без разбора —
одна команда руками (вписано в `mode_analysis.md`). Никогда не роняет вызывающего.

Проверено на живых логах: 6 партий текущего HOB-рана внесены (5W-0L), 6 матчей
заархивированы, повторный запуск +0. `test_match_archive.py` — фикстурный лог:
результат/seat/колода/фильтр событий/дедуп/архив; 14/14 тестов зелёные.

Реестр — ФАКТЫ; категоризация поражений в `<set>_match_log.md` (OUT-ENGINED/AGGRO/…)
остаётся ручным слоем ВЫВОДОВ поверх него.

---

# 9. 📌 ОЖИДАЕТ ДАННЫХ ПО HOB — план на момент, когда 17Lands выложит датасет

**Заведено 18.08.2026 по указанию пользователя: «пока сохрани, как выйдут данные по
hobbit — попробуем».** Всё в § 8.6 и § 8.7 измерено на **MSH**. По HOB датасета ещё нет,
поэтому ни один вывод оттуда на HOB не переносится автоматически (§ КАЛИБРОВКА закон 1
прямо это запрещает).

### Триггер — одна команда

```bash
python3 ~/.claude/skills/mtg-draft-helper/fetch_17l_games.py hob --check
```
Когда ответит размером — рядом почти наверняка появится и draft-датасет (пики):
`fetch_17l_picks.py hob --check`, дальше § 8.9 повторяется на HOB парой команд
(`fetch_17l_picks.py hob` → `pick_quality.py hob --stage 1` и `--stage 2`).
На 18.08.2026 отвечает `HTTP 403` (сет вышел 11.08). Как только ответит размером —
план ниже исполним. Проверять раз в неделю-две; MSH-датасет для сравнения уже скачан
в `games/msh_decks.json.gz`.

### Шаг 1. Забрать данные

```bash
python3 fetch_17l_games.py hob                 # -> games/hob_decks.json.gz
```
Стримит, на диск кладёт только свёрнутую таблицу колод. На MSH заняло 20 секунд:
377 514 партий → 83 457 колод → 39 045 завершённых ранов.
Единица анализа — **КОЛОДА**, не партия: общий винрейт по партиям завышен (56.1%),
потому что победитель играет 7–9 партий, а провал 3.

### Шаг 2. Повторить оба замера и сверить с эталоном MSH

**Условные и по-карточные замеры теперь одной командой: `python3 axes_vs_outcome.py hob`**
(написан 18.08.2026, § 8.8) — внутри пары, частичные d по GIH/ALSA, верхние ранги,
ridge-остатки со сплит-половинами и цветовым агрегатом.

**(а) Маржинальный, § 8.6.** Трофей (7 побед) против провала (≤1 победа, 3 поражения),
оси считать **боевым** `deck_profile.metrics` — подменяется только `DP.parse_deck`.

**(б) Модельный, § 8.7.** Ridge, **λ подбирается по валидации ДЛЯ КАЖДОЙ модели**.
Без этого сравнение недействительно: с фиксированным λ 3160 парных признаков дали
−0.09 чисто от переобучения, и это чуть не было записано как «связки вредят».

**(в) Цветовой перекос GIH, § 8.8 ③.** На MSH глобальный GIH завышает карты цветов
сильнейшей пары на ~0.4σ и занижает красные на ~0.8σ. Если на HOB воспроизведётся —
это готовая поправка к сравнению карт разных цветов в паке, тем более что парный
фильтр 17Lands на HOB сломан и пар-GIH недоступен.

Эталон MSH, с которым сверять:

| величина | MSH |
|---|---|
| потолок по составу колоды | AUC **0.72** |
| карты аддитивно | 0.719 |
| + оси рубрикатора | 0.729 (**+0.010**, на границе шума) |
| + парные связки | 0.689 (**−0.030**, не помогают) |
| только оси, без карт | 0.586 |
| средний GIH, трофей/провал | 57.84 / 56.85, **d=+0.67** |
| ALSA (независим от исходов) | 4.62 / 4.79, **d=−0.49** |
| 7 осей маржинально | все \|d\| ≤ 0.11 |

### Что считать подтверждением, а что опровержением

· **Подтверждение:** оси и на HOB маржинально не различают, качество карт различает,
  потолок около 0.7. Тогда вывод «рубрикатор описывает сборку, а не предсказывает
  результат» становится общим для двух сетов, и его можно вносить в `mode_build.md`.
· **Опровержение:** на HOB какая-то ось даёт \|d\| ≥ 0.3 маржинально ИЛИ оси сверх карт
  дают ≥ +0.03 AUC. Тогда дело в СЕТЕ, а не в осях, и правило должно стать
  сет-зависимым. Это реальный исход: в HOB структурно тонкое removal и мало эвейжна,
  структура там может весить больше.
· **Ни то ни другое:** разница внутри ±0.012 (2σ) — это шум, писать «подтвердилось»
  нельзя.

### Три обязательные оговорки, которые нельзя терять

1. **Цикличность GIH.** Он считается из тех же партий. Поэтому вывод держится не на
   нём, а на **ALSA** — она из порядка пиков, а не из исходов. На HOB повторить
   именно связку GIH+ALSA, иначе результат непроверяем.
2. **Маржинально ≠ условно.** § 8.6 дал «оси не различают», § 8.7 на тех же данных —
   «+0.010 сверх карт». Оба верны, это разные вопросы. Считать ОБА.
3. **Трофей ≠ только колода.** В 7 победах сидят скилл, матчапы и удача. Контроль по
   рангу это ослабляет (на MSH эффект держался во всех шести рангах), но не снимает.

### Шаг 3. Куда двигаться после, если картина повторится

Вопрос «чем отличается победившая колода» упирается в потолок ~0.72 и почти целиком
отвечается качеством карт — для советчика на драфте это тупик. Дальше по убыванию
пользы:

1. ~~**Остатки по картам** (дёшево, данные уже есть на MSH)…~~ **СДЕЛАНО на MSH
   18.08.2026 (§ 8.8 ③) — и в задуманном виде НЕ живёт:** поимённый список на 4/5 шум
   (сплит-половины corr=0.37, выживают 3 из 15 с каждого конца — три бомбы «недооценены»,
   GIH сжимает верх шкалы). Устойчив только агрегат по ЦВЕТУ (перекос силой архетипа).
   На HOB повторять сразу в цветовой форме, поимённый список не обещать.
2. **Качество ПИКА вместо качества колоды** — `draft_data_public.<SET>.<EVENT>.csv.gz`
   (MSH 108 МБ, проверено — лежит). Пик за пиком: содержимое пака и что игрок взял.
   У одного драфта 42 решения против одного исхода — на два порядка больше сигнала.
   Это ровно задача драфт-советчика.
3. **Пилотирование** — `replay_data_public.<SET>.<EVENT>.csv.gz` (MSH 222 МБ). Действия
   в партии, то есть те ~0.28 необъяснённого. `msh_insights.md` давно называет розыгрыш
   боттлнеком на плато.

### До появления данных — что уже изменилось в работе

Рубрикатор не удалён, но статус понижен: **0.586 в одиночку и +0.010 сверх карт — это
описание сборки, а не предсказание результата.** Тир колоды не выдавать как факт;
формулировать «колода устроена как трофейная по таким-то осям», а не «колода на 8».
