---
name: mtg-draft-helper
description: Live MTG Arena draft pick advisor + post-game replay analysis + deck building for Limited. Reads the Arena Player.log, ranks the current pack by 17Lands GIH win-rate, and recommends each pick given pool/colors/curve. Supports The Hobbit (hob, current), Marvel Super Heroes (msh), Secrets of Strixhaven (sos), and Murders at Karlov Manor (mkm). Use when the user wants help drafting in MTG Arena, live pick advice during a draft ("помоги с драфтом", "какую карту брать", "следи за паками", "start a draft"), building/comparing a Limited deck, post-game analysis ("разбери партию", "analyze my game"), or the SOS draft cheat sheet. Do NOT use for Constructed/competitive deckbuilding outside Limited or for non-MTG tasks.
---

# MTG Arena Draft Helper

Live drafting assistant for **MTG Arena Limited**. The user opens packs in Arena; you read the log, rank the pack, and call each pick out loud with reasoning grounded in their pool, colors, and curve. Also does post-game replay analysis and deck building.

**Supported sets:** `sos` (Secrets of Strixhaven, Premier) · `mkm` (Murders at Karlov Manor, Quick) · `msh` (Marvel Super Heroes, Premier) · **`hob` (The Hobbit — вышел на Arena 11.08.2026, текущий)**.

> **✅ HOB: 17Lands ПРОГРЕЛСЯ 16.08.2026** — 146 карт из 188 с выборкой >200 игр. GIH/IWD/ALSA,
> пар-GIH, `⚠trap` и тайбрейк по IWD работают штатно. Обновить: `python3 ~/.claude/skills/mtg-draft-helper/fetch_17l.py hob`
> (с `--check` — только проверить, не перезаписывая).
> **Якоря полосы B по HOB сняты — таблица в `hob_knowledge.md` § ЯКОРЯ ПОЛОСЫ B**, там же
> разделение «якорь vs бомба» (маркер IWD/ALSA ловит оба класса, путать их дорого).
> **✅ Референс-популяция по HOB: 298 листов 7-1/7-2** в `ref_decks/hob/` (16-17.08),
> `hob` добавлен в `draft_live.CALIB`, `build_audit.py` и `learn.py` на HOB работают.
> **Чего нет:** `hob_pick_tiers.json` (ось `пик <тир>` молчит) · пар-фильтрованный GIH
> (17Lands на HOB игнорирует фильтр цветов — см. `hob_knowledge.md`).

All commands use absolute paths so they work from any working directory. Set `SKILL=~/.claude/skills/mtg-draft-helper`.

> ### 📓 ПЕРЕД ЛЮБОЙ ПРАВКОЙ СКИЛЛА — ПРОЧИТАТЬ `JOURNAL.md`
> История изменений, проведённые эксперименты и **действующие эталонные числа** по 298
> трофейным листам. Там же § 5 — дефекты приборов, которые уже находили: три из пяти ложных
> выводов 17.08 породил не анализ, а сломанный измеритель, и повторять этот заход не нужно.
> Числа § 2 привязаны к состоянию приборов; сверка — `python3 $SKILL/test_journal.py`
> (падает, если приборы поправили, а журнал нет).
> **Результат каждого эксперимента, включая отрицательный, дописывать туда же.**

> ### 🔴 НИКОГДА НЕ ПУБЛИКОВАТЬ ДАННЫЕ НА claude.ai (указание пользователя, 16.08.2026)
> Страницы этого скилла — атлас архетипов, индекс связок, replay-отчёты — **только локальные
> файлы**. Не вызывать Artifact, не предлагать «показать через артефакт». Смотреть их через
> локальный сервер: `bun ~/.claude/skills/mtg-draft-helper/serve.ts` (слушает только 127.0.0.1),
> дальше http://localhost:8787/. Собирают страницы `build_atlas.py`, `build_combos_page.py`,
> `replay_moments.py` — все пишут html рядом со скиллом.

## Prerequisite (one-time, user does this)
Arena → **Settings → Account → Detailed Logs (Plugin Support)** = ON, then **restart Arena**. Without it the log has no draft/match data. Log lives at `~/Library/Logs/Wizards Of The Coast/MTGA/Player.log`.

---

# 📂 ЭТОТ ФАЙЛ — ДИСПЕТЧЕР. ТЕЛО РЕЖИМА ЧИТАЕТСЯ ОТДЕЛЬНО

Файл разрезан 16.08.2026. До этого он весил 871 строку / ~18 000 слов и грузился целиком
перед **каждым** пиком — при том, что живой драфт специально держат на Sonnet 5 / effort
medium ради скорости, а больше половины текста к пику не относилась (сборка, разбор партии,
Sealed, заведение сета, MSH-специфика на HOB-драфте). Не сливать обратно в один файл.

| Что делаем | Файл | Когда читать |
|---|---|---|
| **Живой драфт, пики** | **`mode_draft.md`** | ПЕРЕД первым пиком |
| Сборка колоды, рубрикатор, Sealed | `mode_build.md` | после `DRAFT COMPLETE` (рубрикатор — ещё и на аудите P2P1/P3P1) |
| Разбор партии, живой коучинг в матче | `mode_analysis.md` | после матча / перед матчем |
| Завести новый сет | `setup_new_set.md` | разово |
| Снятые и опровергнутые правила | `rules_graveyard.md` | при пересмотре правил, НЕ в работе |

**Плюс к `mode_draft.md` перед каждым драфтом:** **`<set>_patterns.md` (механизмы: связки,
движки, сквозные закономерности — извлечено мультиагентным разбором)** · **`<set>_learned.md`
(что реально играют победители: ядро каждой пары, роли, кривая)** · `<set>_knowledge.md` (драфт-мета) ·
`<set>_insights.md` (что выигрывает в партиях) · читшит сета (`hob_cheat.md` / `msh_cheat.md` /
`draft_cheat.md`) · `<set>_archetypes.json` (линии сборки).

> ### 🧠 КАК СКИЛЛ УЧИТСЯ (внесено 16.08.2026 по указанию пользователя)
> **GIH — ТОЛЬКО ось сортировки пака. Мерилом колоды он не является и в сравнениях сборок
> не участвует.** Оптимизация по нему даёт колоду, которая нигде не плоха — то есть мягкую
> середину; это подтвердилось на пяти трофейных листах HOB, где у победителей средний GIH
> оказался НИЖЕ нашего во всех парах сразу, а карт «ниже 55» — до двенадцати на колоду.
> Ось «средний GIH» из `build_audit.py` по этой причине удалена.
>
> **Знание о том, КАК собирать, скилл добывает сам из увиденных колод:**
> ```bash
> # пополнить популяцию с untapped.gg (топ-N трофейных листов Gold+, дедуп по содержимому) →
> python3 ~/.claude/skills/mtg-draft-helper/fetch_trophy_decks.py <set> --n 10
> python3 ~/.claude/skills/mtg-draft-helper/learn.py <set>      # пересобрать <set>_learned.md
> ```
> `fetch_trophy_decks.py` заменяет ручной перенос листов — **им и был ограничен размер выборки**,
> из-за чего выводы по HOB дважды умирали от новой порции данных (`hob_knowledge.md`). Ключи:
> `--sort record` (сначала 7-0), `--rank`, `--event`, `--dry-run`. Провенанс — `ref_decks/<set>/_source.json`.
> `learn.py` извлекает по каждой ПАРЕ: ядро (карты, которые есть во ВСЕХ колодах пары),
> роли, кривую, число копий, земли — и общее по сету (диапазоны ролей, универсальные карты,
> максимум копий). Ничего из этого не считается по GIH.
>
> **ЗАЧЕМ карта в колоде — отдельный канал (внесено 16.08.2026).** Частота отвечает «эту
> играют 4 из 4», но не «с чем она работает», а в паке нужно именно второе. Механизмы
> извлекаются мультиагентным разбором: агенты читают листы победителей И оракл-тексты,
> затем независимые скептики пытаются каждую находку ОПРОВЕРГНУТЬ по тексту карт
> (из 161 заявленной связки HOB отбраковано 52).
> ```bash
> python3 consensus.py <set> --save     # модальный лист каждой пары (по частоте, не по GIH)
> ```
> · `<set>_patterns.md` — читать перед драфтом: сквозные закономерности, движки по цветам,
>   правила пика по парам, раздел «что противоречит интуиции»;
> · `<set>_combos.json` — машиночитаемо, **печатает `⚑ СВЯЗКА` В МОМЕНТ ПИКА**
>   (`draft_live.combo_banner`): когда часть связки уже в пуле, а недостающая деталь лежит
>   в текущем паке. Двойной фильтр — механизм подтверждён текстом карт И карты встречаются
>   вместе чаще случайного (lift ≥ 1.4 по всей популяции). Это единственный канал, который
>   работает без участия модели.
> · **на СБОРКЕ всё это сводит `pool_dossier.py`** (внесено 20.08.2026, JOURNAL § 8.20):
>   для каждой карты пула — роли, частота+потолок копий у победителей пары, связки и
>   АНТИ-связки с картами этого пула, флаги ловушек; плюс таблица линий (запас по маршрутам
>   § 8.17). Вход одиночной сборки и оркестратора (`mode_build.md` § МУЛЬТИСБОРКА).
>
> **Разделение файлов:** `<set>_learned.md` и `<set>_patterns.md` — автогенерация, правки
> руками не живут · `<set>_insights.md` — выводы из НАШИХ партий, вручную ·
> `<set>_knowledge.md` — драфт-мета.
> Пополнять `ref_decks/<set>/` при каждой встреченной трофейной колоде: одна колода на пару —
> это наблюдение, а не норма, и файл сам об этом предупреждает.

**Перед матчем:** `<set>_pilot.md`. **Для живого коучинга:** `live_advice_rules.md`.

---

# 🔴 ХАРД-РУЛЫ ЖИВОГО ЦИКЛА (дублированы здесь намеренно — это поведение агента, не игра)

## 1. НИКОГДА НЕ ЗАКАНЧИВАЙ ХОД ПОСЛЕ ВЫДАЧИ ПИКА (баг пойман 16.07.2026, дважды за драфт)
**Текст пика и следующий вызов `watch` идут в ОДНОМ сообщении**: выдал блок совета — и тут же,
без паузы и без закрывающего вопроса, вызвал `watch` в том же ответе. Закончишь ход на тексте —
харнесс встанет и будет ждать пользователя, цикл умрёт, и человеку придётся печатать «пак»
каждый пик. **Это единственный режим отказа этого цикла, и он твой, а не скрипта.**
- **НЕ** спрашивать «готов к следующему?», «продолжаем?», «скажи когда» — ход не отдавать.
- **НЕ** ждать подтверждения, что пик сделан — `MTGA_SETTLE` уже это дебаунсит.
- Цикл кончается ТОЛЬКО на `DRAFT COMPLETE`, 10-минутном таймауте без пака или прерывании пользователем.
- Проверка перед каждой отправкой в живом драфте: *«прикреплён ли к этому сообщению вызов `watch`?»*
  Нет → цикл сломан.

## 2. СНАЧАЛА ОПРЕДЕЛИ РЕЖИМ — у Premier и Quick РАЗНЫЕ СКРИПТЫ
Arena пишет их разными событиями: Premier — `PackCards`/`DraftPack`, Quick (боты) —
`BotDraftDraftStatus`. **Premier-парсер физически не видит Quick-драфт и наоборот.**
Не знаешь режим — спроси одним словом.

| режим | команда (блокирующая, `timeout: 600000`) |
|---|---|
| **Premier / Traditional** | `MTGA_SETTLE=1 python3 ~/.claude/skills/mtg-draft-helper/draft_live.py <set> watch [fresh]` |
| **Quick Draft (боты)** | `MTGA_SETTLE=1 python3 ~/.claude/skills/mtg-draft-helper/quickdraft_watch.py <set> [fresh]` |

`fresh` — только на самом первом вызове драфта. **Вывод у обоих ИДЕНТИЧЕН** — оба идут через
общий `draft_live.render_block`; различается только парсер лога. Паритет закреплён тестом
`test_parser_parity.py` — **правишь рендер, правь в `render_block`, а не в копии.**

## 3. Модель на живом драфте — Sonnet 5, effort medium
Замерено на `draft_sim.py`: Opus / high / xhigh давали 20–55 с на пик, игрок пикал быстрее,
чем приходил совет. Для разбора партии и сборки часов нет — там Opus/high уместен.

> **ТЕСТЫ ИНСТРУМЕНТА ЗАПУСКАТЬ НА ТОЙ ЖЕ КОНФИГУРАЦИИ** (указание пользователя 17.08.2026).
> Переигровка драфта 31a78cee шла на Opus и дала отрицательный результат — но вывод
> непроверяем: Opus и без подсказок вытягивает пик, и он же успевает прочитать 659 строк
> `hob_patterns.md`, чего Sonnet/medium под пик-клоком скорее всего не успевает.
> Проверять инструмент надо в тех условиях, в которых он работает:
> `agent(prompt, {model: 'sonnet', effort: 'medium'})`.

---

## 🏛️ КАЛИБРОВКА — ЗАКОН НАД ВСЕМИ ПРАВИЛАМИ ВО ВСЕХ ФАЙЛАХ ЭТОГО СКИЛЛА (внесено 10.08.2026)

> **Все пороги, квоты и стоп-краны выведены из НАШИХ ПОРАЖЕНИЙ. Это модель, обученная на собственных провалах, и она никогда не проходила отрицательный контроль.** Первая же проверка на внешней выборке показала цену: **наш рубрикатор отбраковал бы 10 из 14 реальных колод 7-1/7-2 (на выборке n=23 — 16 из 23)** — по стоп-крану «существо к T2 ≥60%» (у них медиана 54.3%) и по порогу «removal ≥4» (у них медиана 1, у пяти колод ноль). Мы измеряли «похоже ли это на нашу колоду», а не «выигрывает ли это».

**Отсюда четыре закона, приоритетных над любым конкретным порогом в любом файле:**

**1. Источник калибровки — популяция победителей, а не наши сливы.** `ref_decks/` — **298 листов 7-1/7-2 по HOB** (`hob` есть в `draft_live.CALIB`; MSH-листов в каталоге НЕТ, они были заменены). `build_audit.py` сравнивает колоду **внутри её собственной пары**. ⚠️ Числа `n=23`, встречающиеся ниже и в `mode_build.md` (медиана T2 54.3%, эталон отклонения +0.49/+0.96, кластеры ⚑ПЛАН), сняты на СТАРОЙ MSH-выборке и по 298 HOB-листам не пересчитаны — читать их как ориентир другого сета, а не как эталон HOB (помечено 20.08.2026). Фиксированные пороги заменяются вопросом «где моя колода в распределении». Дефект — только **выход ниже ВСЕЙ популяции**, не отклонение от выдуманного числа.
```bash
python3 ~/.claude/skills/mtg-draft-helper/build_audit.py <мой_лист.txt> --pool pools/<set>_<draft8>.txt
python3 ~/.claude/skills/mtg-draft-helper/deck_profile.py <лист> [--brief]
python3 ~/.claude/skills/mtg-draft-helper/cut_analysis.py                  # что победители режут на цвете
```
**Пул сохраняется САМ — руками ничего делать не надо.** `draft_live.py` пишет полный пул в `pools/<set>_<draft8>.txt` **на КАЖДОМ пике** и печатает `💾 пул сохранён: …`. Автоматически — потому что пул физически живёт только в `Player.log`, а он **ротируется быстро**: после ротации остаток пула восстановить неоткуда.

**2. Тест процесса: наш мейн не должен совпадать с «жадным» топ-23 по GIH.** Ни одна из 23 победивших колод не равна жадному списку — все отдают **+0.49 GIH на карту, 23 из 23 в одну сторону** (минимум +0.03), случайно ≈ 1 шанс из 16 000. Совпадение с жадным = «план не выбран, пул отсортирован». Печатает `build_audit.py`. **Этот тест про НАШ процесс, поэтому он не страдает от survivorship-bias референс-выборки** — в отличие от любых выводов вида «4 цвета выигрывают», которые из выборки только победителей делать НЕЛЬЗЯ.

**3. Почему отклонение от GIH обязано быть — и куда именно.** GIH это **среднее по всем колодам**, где карта встретилась, то есть маргинальная величина, а не условная на моей сборке. Поэтому он систематически:
   - **завышает** карты, одинаково приличные везде (дженерик-тела, трюки, дженерик-добор): Agent Maria Hill 60.3, Take Up the Shield 59.9, Brave Brawler 59.2, Crowd of True Believers 57.1 (мейн 0/3), Depower 56.5 (0/3);
   - **занижает** карты, ценность которых условна на плане: пейоффы движка/трайбала (Machinesmith 51.3, Training Regimen 52.4, Invisible Woman 46.4) и **фикс** (Restorative Technique 52.1 — в двухцветной колоде он мёртв, оттого и среднее низкое).

   **Драфт по GIH строит колоду, которая нигде не плоха — то есть буквально мягкую середину**, которую наш же матч-лог называет главным диагнозом (10 из 17 поражений OUT-ENGINED). Величина поправки мала (≈0.5 на карту), направление — всегда в сторону пейоффа собственного плана.

**4. Правила ОБЯЗАНЫ устаревать.** Каждое новое правило пишется с **n и датой**. Правило, выведенное из 1–7 наших партий, имеет статус гипотезы и проверяется на `ref_decks/` при первой возможности; **противоречит популяции — понижается или удаляется, а не остаётся «на всякий случай»**.
> **Куда удалять: в `rules_graveyard.md`, а не в пометку «СНЯТО» по месту.** До 16.08.2026
> опровергнутые правила оставались в рабочем тексте с объяснением, почему они неверны, и
> объяснение регулярно выходило длиннее действующей формулировки. Это ровно та же болезнь,
> что «файл рос 2 месяца, не удалив ни одного правила», просто в новой форме.

---

## Как этим пользоваться

Пользователь говорит «помоги с драфтом» / «следи за паками» → определить режим (хард-рул 2) →
прочитать `mode_draft.md` + файлы знаний сета → запустить блокирующий `watch` → пик → `watch` →
… до `DRAFT COMPLETE` → `mode_build.md`.

«Разбери партию» → `mode_analysis.md`. «Собери колоду» / Sealed-пул текстом → `mode_build.md`.
